# PROG-20260909：T0 来源登记与 V3 后续实施条件规划

- 日期：2026-09-09
- 当前 Phase：V3 下游实施准备（M0 未收口，M1 未开工）。
- 状态：`completed`（T0 来源登记规范化与后续实施条件规划完成；V3 实现未开始）
- 关联需求：[REQ-20260906-04](../requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md)
- 关联缺陷：[BUG-20260908-01](../bugs/BUG-20260908-01-v3-downstream-contract-source-hash-normalization.md)
- 前次进度：[PROG-20260908](PROG-20260908.md)

## 本轮任务边界

执行 T0：把已经验证的上游提交树、历史观察和运行时规范化哈希明确登记，
关闭 BUG-20260908-01 的来源登记问题。
仅修改文档与测试；不进入 V3 M1，不修改业务规则或候选生成逻辑；
不连接 MongoDB、SQL Server 或在线模型；不读取 `.env`；不提交、不推送。

用户随后要求把 Agent 2 真实生成所需条件写入规划，并确认固定私有资料中的现有事实无需再次
确认。本日追加任务只修改公开规划与进度，不读取凭据、不连接数据库或 provider，也不实现代码。

## 重新核实上游证据（只读）

从同级 RuleReader 仓库 Git 提交树读取，未用工作区文件冒充：

| 类别 | 字节来源 | 大小 | 行尾 | SHA-256 |
| --- | --- | --- | --- | --- |
| 提交树原始字节 | `git show bad6fd3:contracts/...schema.json` | 29870 | LF | `0e39c7acd96b22fc91c3a6a228561d3db9b6368db9f062c95f970b19ec164903` |
| 工作区原始字节 | SqlBot 冻结副本 | 31198 | CRLF | `2c5e4603cda243a84f5ec1c47ee65d3e377598fed86b1ddab5a167a2c342c566` |
| 运行时规范化字节 | 加载器 CRLF 规范化后 | — | CRLF | `2c5e4603cda243a84f5ec1c47ee65d3e377598fed86b1ddab5a167a2c342c566` |

- 完整 commit：`bad6fd349a6ecbff190b9bd0ac1bc34a48588325`
- RuleReader HEAD：`01ddae0979e8adb4fcb41e68e3f33deb850b1443`
- SqlBot HEAD：`ca09f4f6a7c836a410d6c48103cba1047b2d782b`
- 两端 parsed JSON 相等。

## 修改内容

1. **来源清单** `docs/specs/fact-binding-request-3.0.0-source.json`：
   - 保留全部历史观察字段；
   - 新增 `verifiedCommitTree`（完整 commit、HEAD、字节长度、行尾、SHA-256、结构相等）；
   - 新增 `runtime`（规范化算法、行尾、SHA-256、与常量一致）；
   - 新增 `sha256Note` 明确顶层哈希语义。
2. **契约测试** `tests/contract/test_fact_binding_v3_contract.py`：
   - `test_upstream_schema_source_record_is_frozen_and_machine_readable` 同步更新，
     断言历史字段不变、`verifiedCommitTree` 完整 commit、提交树 SHA-256/字节长度、
     `runtime.sha256 == FACT_BINDING_SCHEMA_SHA256_V3`。
3. **BUG-01** 状态改为 `completed`，写入修复内容与验证证据。
4. **后续实施规划**：同步更新 REQ/BIZ/DEV、BUG-02、ROADMAP 与 README，记录事实无需重复确认、
   私有事实到新 handoff/context/snapshot/grant 的转换流程、M0 当前状态和真实调用完整条件。

## 未修改

- 打包 Schema、`handoff_intake_v3.py`、`FACT_BINDING_SCHEMA_SHA256_V3`、`.gitattributes`；
- V2/V3 领域或应用代码；RuleReader、RuleDataReferences；其他测试和文档。

## 验证

- `uv run pytest tests/contract/test_fact_binding_v3_contract.py`：15 passed；
- `uv run ruff check .`：通过；
- `uv run ruff format --check .`：190 files already formatted；
- `uv run pytest`：424 passed（含 V3 contract 15 项与 V3 handoff intake 15 项）；
- `git diff --check`：通过。
- 本日累计变更涉及的 15 个 Markdown 文件：293 个相对文件链接、0 个断链；只验证目标文件
  存在性，未逐项解析 Markdown 锚点。

## 仍开放

### M0 当前唯一剩余门禁

- `REQ/BIZ/DEV-20260906-04` 完成人工批准；
- 规划文档纳入 Git 提交。

### M1 门禁

- context/snapshot 批准记录载体；
- 维护流程；
- metadataReview 与 SqlBot 的契约交接方式冻结。

### 真实 M3/M6 门禁

- `BUG-20260908-02` 涉及的事实使用新版本 catalog、规则和 handoff；
- 已批准 context/snapshot/grants；
- MongoDB 同次调用仓储背书；
- provider 配置和当次在线调用授权。

### 真实 batch

- 上游进度已有持久化及回读记录；
- 本轮未实时连接 MongoDB，因此不宣称当前数据库状态；
- 真实调用时仍必须重新读取并完成仓储背书。

### 机器契约完整设计

- 属于规划文档批准前需要收口的设计内容；
- 不单独伪装成新的外部 M0 门禁。

本任务仅完成来源登记规范化与后续实施条件规划；V3 实现未开始。

## V3 后续实施与真实调用条件规划（2026-09-09 追加）

用户确认固定 `RuleDataReferences` bundle 中实际存在的 Excel 与其他资料内容均为已确认事实；
滞后的状态标签不否定已有内容。后续不得要求用户重复确认或重新提供这些事实。只有完整检查固定
资料后确实不存在的信息，才能登记为待补充。

本轮已同步更新：

- [REQ-20260906-04](../requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md)：增加已确认
  事实的消费要求，刷新当前阻断项与真实调用条件；
- [BIZ-20260906-03](../decisions/BIZ-20260906-03-v3-downstream-authority-boundary.md)：记录“事实
  已确认、运行授权仍独立”的权威边界；
- [DEV-20260906-04](../architecture/DEV-20260906-04-v3-downstream-pipeline-alignment.md)：增加私有事实
  到 handoff/context/snapshot/grant 的转换流程，以及调用 Agent 2 生成 V3 candidate 的完整门禁。

事实资料已足够，后续主要工作为：

1. `businessRuleReview` 基于固定资料修订受影响的上游表达，生成新 catalog digest、规则版本和
   handoff；不覆盖已落库版本。
2. `metadataReview` 把已确认字段、对象、枚举和关系转换为版本化 V3 context、snapshot 与精确
   grants，并形成批准记录。
3. SqlBot 按 M1–M6 实现独立 V3 解析、候选生成、静态门禁、存储和调用入口。
4. 真实调用时按精确 `ruleVersion + requestId` 完成 MongoDB 仓储背书，并取得当次在线 provider
   授权；输出始终为不可执行、待审核 candidate。

R4 仍是局部语义对齐任务，但不再是“需要用户补资料”：由 `businessRuleReview` 使用固定私有来源
完成版本化证据裁决；裁决完成前仅阻断该事实。

结论：现有固定私有资料已足以继续后续工程，不需要用户再次提供或确认其中已经存在的业务事实、
字段、对象、枚举或关系。后续若出现阻塞，应先证明固定资料中确实没有对应内容；批准记录、最小
权限配置和真实 provider 调用授权属于工程治理或运行授权，不属于事实补充。

## 待用户批准事项（proposed，未经批准不得实施）

以下为 V3 下游实施前的关键设计决定，需用户人工审查并批准。每项给出 proposed 决定、采用理由、
替代方案、风险及批准后的下一动作。

> 当前文档完成后只是具备人工审批条件。用户批准前不得将 REQ/BIZ/DEV 更新为 approved；
> 用户批准设计不自动授权 commit/push/MongoDB/在线模型；M0 在文档批准并得到 Git commit 授权后
> 才能正式收口；M1 完成后保持未提交供审查。

### 1. 下游对象版本表

- **proposed**：采用 DEV §4.3 版本表——`FactBindingRequest.contractVersion = 3.0.0`；
  `SqlTemplateCandidateV3.schemaVersion = 3.0.0`；
  `HandoffClosureV3`/`ProjectBindingContextV3`/`GovernedMetadataSnapshotV3`/`ApprovalRecordV3`/
  `ResolveMetadataRequestV3`/`BindingResolutionReportV3`/`GenerateSqlCandidateRequestV3`/
  `ValidateSqlCandidateRequestV3`/`SqlStaticValidationReportV3`/存储包装首版均为 `1.0.0`；
  `ruleRef.schemaVersion="3.0.0"` 表示上游规则契约版本。共 12 个独立顶层契约对象。
- **理由**：延续对应 V2 对象的既有版本先例，自身契约独立演进；`SqlTemplateCandidateV3` 采用 `3.0.0`
  延续候选契约与生成管线世代对齐的既有先例；避免机械继承 FBR 版本号。
- **替代方案**：所有下游对象机械继承 `3.0.0`（被否决：会混淆"自身契约版本"与"所引用规则契约版本"，
  破坏版本独立性）。
- **风险**：版本号数字相同（`1.0.0`）可能引起误解；通过明确"自身契约 vs 所引用规则契约"分界缓解。
- **批准后下一动作**：当前已作为 proposed 写入 REQ/BIZ/DEV；用户批准后更新文档状态，
  M1 契约测试绑定各对象 `schemaVersion`。

### 2. context/snapshot 批准记录载体与信任边界

- **proposed**：采用 DEV §3.3 的 `ApprovalRecordV3` 结构（`approval_id` 是不透明稳定 ID，**不是
  SHA-256**；`context_ref`(context_id, context_version, sha256)/`snapshot_ref`(snapshot_id,
  snapshot_version, sha256)/`policy_version`/`actor_ref`(wire actorRef)/`approved_at`/
  `content_sha256`），insert-only，独立于 SQL 候选审批。
- **理由**：metadataReview 是批准 owner，SqlBot 是契约实现 owner；需要不可变审计记录精确绑定
  context/snapshot 的 ID、版本和内容哈希（三要素缺一不可）。
- **信任边界分层**：M1 只负责内容闭包校验（格式、哈希、引用、声明状态内部一致）；
  真实 M3/M6 还需受信批准来源核验（DEV §3.6）——通过受控只读端口按 approvalId 取得 metadataReview
  登记的记录并逐字段比对；元数据内容已确认不等于任意客户端 JSON 已获运行授权。
- **替代方案**：复用 V2 的 `approvalRef` 字段（被否决：V2 的 approvalRef 仅是一个引用，不包含
  完整的版本链和载荷哈希，无法满足 V3 独立契约链的审计要求）。
- **风险**：批准记录存储设计（MongoDB 集合/索引）在 M6 编排前才冻结；早期可能需迁移。
- **批准后下一动作**：当前已作为 proposed 写入 DEV §3.3/§3.6；用户批准后更新文档状态，
  M1 契约测试覆盖其字段，真实 M3/M6 核验实现按 §3.6 门禁要求另立阶段。

### 3. V3 独立契约链

- **proposed**：采用 BIZ 第 1 条决策——V3 下游是独立契约链，不是 V2 链的扩展；禁止 V3↔V2 转换、
  包装或降级；V2 链行为逐字节不变。
- **理由**：V2/V3 字段集互不相交、双向严格解析必然失败、上游禁止降级；不存在合法 V3→V2 桥。
- **替代方案**：在 V2 链中增加 V3 适配层（被否决：违反 BIZ 第 1 条，会吞掉 V3 五阶段/优先级/多
  outcome 语义）。
- **风险**：V3 需独立实现完整链路（Phase 2G→3→4→存储），工程量较大；通过 M1–M6 分阶段交付缓解。
- **批准后下一动作**：当前已作为 proposed 写入 BIZ；用户批准后更新文档状态，M1 开始实现 V3 专属模块。

### 4. 复杂事实拆分责任

- **proposed**：Agent 1/businessRuleReview 负责业务表达和派生语义（包括 R5/R6/R7/R8/合并组的
  业务条件）；Agent 2 只为 source/aggregate/exists 基础事实生成单事实 SQL；不假设数据库存在
  `*_eligible` 物理列；集合型或复合条件必须在上游 vNext 中拆分为可追溯基础事实。
- **理由**：AST 只能证明参数/对象/列/join/`fact_value` 来源，不能证明规则业务语义；复杂布尔事实
  必须由规则执行层表达。
- **替代方案**：Agent 2 直接消费预计算 `*_eligible` 布尔列（被否决：当前 handoff 的
  `mappingCandidate` 为 unresolved，现有已核验证据没有提供可授权的物理映射；不得假设存在
  对应物理列，且会把业务规则藏进不受校验的自然语言说明中）。
- **风险**：上游 vNext 交付延迟会影响 M3 输入可生成性；通过 BUG-02 跟踪并独立裁决 R4 缓解。
- **批准后下一动作**：当前已作为 proposed 写入 BIZ/DEV；用户批准后更新文档状态，上游 vNext 启动。

### 5. M1–M6 实施顺序与真实调用门禁

- **proposed**：采用 DEV §12 里程碑分层——离线 M1–M3 使用合成 fixture/fake provider，不依赖真实
  数据库或在线授权；真实 M3/M6 必须等待 BUG-02 新版本 catalog/rules/handoff、已批准
  context/snapshot/grants、同次 MongoDB 仓储背书、provider 配置和当次在线授权。
- **理由**：离线开发可并行于上游 vNext 和批准流程；真实调用必须满足全部门禁。
- **替代方案**：所有里程碑都等真实 batch 落库后再开发（被否决：严重拖慢进度，且离线测试不依赖真实数据）。
- **风险**：真实 M3/M6 启动时间受上游 vNext 和批准流程影响；通过提前准备合成 fixture 缓解。
- **批准后下一动作**：当前已作为 proposed 写入 REQ/BIZ/DEV；用户批准后更新文档状态，M1 开工。

## 批准后第一个 M1 实施提示词

> 按 DEV §12 M1 与 §2 文件清单，实施 V3 下游 M1：
>
> **M1 允许的五个实现文件固定为：**
> 1. `src/release_sql_bot/domain/project_bindings_v3.py`（`ProjectBindingContextV3`/
>   `GovernedMetadataSnapshotV3`/`ApprovalRecordV3` 及 context/snapshot/approval 嵌套引用与 grant 模型；
>   `ApprovalClosureValidationErrorV3` 异常定义在同文件）
> 2. `src/release_sql_bot/domain/handoff_closure_v3.py`（`HandoffClosureV3`/
>   `RepositoryVerifiedHandoffV3`）
> 3. `src/release_sql_bot/application/validate_approval_closure_v3.py`（V3 专属纯函数；
>   V2 模块不得导入；无 wire schemaVersion；无外部副作用）
> 4. `tests/contract/test_project_bindings_v3_contract.py`
> 5. `tests/contract/test_handoff_closure_v3_contract.py`
> 不得使用仓库根目录下的 `application/` 路径。
>
> **禁止修改：** V2 模块、既有测试期望、配置、Schema、加载器常量、M2/M3 应用代码。
>
> **M1 范围：** 仅实现契约与纯计算校验；不实现 MongoDB 仓储、生命周期事件存储或应用解析。
> `ResolveMetadataRequestV3` 的应用解析实现属于 M2，M1 不得进入。
>
> **`validate_approval_closure_v3` 纯函数（M1 核心）：**
> - 签名：`validate_approval_closure_v3(project_context, metadata_snapshot, approval_record) -> None`
> - 直接接受 `ProjectBindingContextV3`、`GovernedMetadataSnapshotV3`、`ApprovalRecordV3`；
>   不是跨版本共享函数，V2 模块不得调用或导入；可复用只有 canonical SHA-256 等底层算法；
> - 成功返回 `None`；任一九组检查失败时抛出 `ApprovalClosureValidationErrorV3`（仅含稳定中性 issue code）；
> - 不访问 MongoDB/provider/SQL Server/环境变量；不修改输入对象；
> - 验证逻辑即 DEV §5.2 所列九个有序检查组；
> - 实际载荷哈希使用 `application/canonical.py` 的 `canonical_content_sha256` 约定（camelCase、
>   JSON 模式、仅排除根级 contentSha256、不排除嵌套 sha256 和业务字段、不复用 handoff 特殊算法）；
> - 成功只证明内容闭包内部一致，不证明批准真实性，真实 M3/M6 必须通过独立受信批准来源核验（DEV §3.6）。
>
> **`test_project_bindings_v3_contract.py` 最低覆盖（除现有 12 项外，再明确）：**
> - 九个有序检查组（DEV §5.2）各有一个精确反例；
> - 多重错误输入验证 fail-fast 顺序（同一输入存在多个错误时，按 §3.5 顺序返回第一个）；
> - 异常文本不包含测试输入中的 ID、哈希或载荷值；
> - V2 模块不导入 V3 纯函数（静态断言）；
> - 纯函数调用前后输入对象序列化结果完全一致；
> - 修改 context/snapshot 业务内容但保留旧自哈希和批准引用，必须失败；
> - 只修改 `context.authorizationPolicyVersion`，必须失败。
> 注意：`approval_id` 是不透明稳定 ID，**不是 SHA-256**。
>
> **`test_handoff_closure_v3_contract.py`：** 仍只测试 handoff 闭包，不混入 approvalRecord 测试。
>
> **测试：** 定向测试（2 文件）全部通过；全量 `uv run pytest` 通过数不低于 424。
>
> **Git 边界：** M1 完成后保持未提交供人工审查；commit 需要用户另行明确授权；push 需要用户另行
> 明确授权；M0 设计批准不授权数据库或在线模型调用。

## M0 审批包与精确提交准备（2026-09-09 追加）

### 五项设计审批摘要

以下为 V3 下游实施前的关键设计决定，需用户人工审查并批准。每项只写当前 proposed 决定、
对应文档章节、批准后的具体影响。未经用户明确批准，所有文档保持 proposed。

> 业务事实已确认，不再要求用户确认私有资料已有内容。

#### 1. 12 项版本表

- **proposed**：`FactBindingRequest.contractVersion = 3.0.0`；`SqlTemplateCandidateV3.schemaVersion = 3.0.0`；
  其余 10 个对象首版 `schemaVersion = 1.0.0`（含 HandoffClosureV3、ProjectBindingContextV3、
  GovernedMetadataSnapshotV3、ApprovalRecordV3、ResolveMetadataRequestV3、BindingResolutionReportV3、
  GenerateSqlCandidateRequestV3、ValidateSqlCandidateRequestV3、SqlStaticValidationReportV3、存储包装）。
  共 12 个独立顶层契约对象。
- **对应章节**：DEV §4.3
- **批准后影响**：M1 契约测试绑定各对象 `schemaVersion` 常量；版本独立性测试（矩阵 13）。

#### 2. ApprovalRecordV3、九组内容校验与独立批准真实性门禁

- **proposed**：采用 DEV §3.3 的 `ApprovalRecordV3` 结构；DEV §5.2 九个有序检查组按顺序 fail-fast；
  DEV §3.5 九个 issue code；`validate_approval_closure_v3` 为 V3 专属纯函数（内容闭包）；
  DEV §3.6 真实 M3/M6 受信批准来源门禁（proposed，实现属于后续阶段）。
- **对应章节**：DEV §3.3、§3.5、§3.6、§5.2
- **批准后影响**：M1 实现五文件范围（含异常类）；真实 M3/M6 核验实现按 §3.6 另立阶段。

#### 3. V3 独立契约链

- **proposed**：V3 下游是独立契约链，不是 V2 链的扩展；禁止 V3↔V2 转换、包装或降级；V2 链行为逐字节不变。
- **对应章节**：BIZ 第 1 条
- **批准后影响**：M1 开始实现 V3 专属模块；V2 全量回归零变化。

#### 4. Agent 1、metadataReview、SqlBot 职责

- **proposed**：Agent 1/businessRuleReview 负责业务表达和派生语义；metadataReview 负责物理绑定、
  entity-key/join 授权及 context/snapshot 产生与批准；SqlBot 只为 source/aggregate/exists 基础事实
  生成单事实 SQL；AST 不证明规则业务语义。
- **对应章节**：DEV §3.4
- **批准后影响**：上游 vNext 启动；M1 契约代码不越界消费业务规则。

#### 5. M1–M6 顺序、离线与真实调用边界

- **proposed**：离线 M1–M3 使用合成 fixture/fake provider；真实 M3/M6 必须等待 BUG-02 新版本
  catalog/rules/handoff、已批准 context/snapshot/grants、同次 MongoDB 仓储背书、provider 配置和
  当次在线授权。
- **对应章节**：DEV §12
- **批准后影响**：M1 开工；真实 M3/M6 启动时间受上游 vNext 和批准流程影响。

### 精确拟提交文件/补丁清单

只准备清单，不执行 git add/commit。

#### T0 来源登记（独立提交）

| 文件 | 状态 | 内容 | 依赖 | 混合 |
| --- | --- | --- | --- | --- |
| `docs/specs/fact-binding-request-3.0.0-source.json` | 修改 | verifiedCommitTree + runtime 三类哈希登记 | 无 | 否 |
| `tests/contract/test_fact_binding_v3_contract.py` | 修改 | 来源记录测试同步更新 | 依赖上方 JSON | 否 |

#### V3 M0 规划文档

| 文件 | 状态 | 内容 | 依赖 | 混合 |
| --- | --- | --- | --- | --- |
| `docs/architecture/DEV-20260906-04-v3-downstream-pipeline-alignment.md` | 修改 | V3 下游管线对齐设计与实施计划（核心） | 无 | 否 |
| `docs/decisions/BIZ-20260906-03-v3-downstream-authority-boundary.md` | 修改 | V3 下游权威边界与复用决策 | 无 | 否 |
| `docs/requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md` | 修改 | V3 下游管线对齐需求 | 无 | 否 |
| `docs/progress/PROG-20260909.md` | 新增 | T0 来源登记 + V3 后续实施条件规划 + 审批准备 | 无 | 否 |
| `docs/bugs/BUG-20260908-01-v3-downstream-contract-source-hash-normalization.md` | 新增 | 来源登记缺陷（已修复） | 无 | 否 |
| `docs/bugs/BUG-20260908-02-v3-upstream-fact-catalog-contract-gaps.md` | 新增 | 上游业务表达缺口 | 无 | 否 |
| `docs/bugs/README.md` | 修改 | BUG 索引更新 | 依赖两个 BUG 文件 | 否 |

#### 需要选择提交范围（混合内容）

| 文件 | 状态 | V3 M0 内容 | 混合内容 | 建议 |
| --- | --- | --- | --- | --- |
| `README.md` | 修改 | V3 M0 文档导航链接 | Phase 6 审核发布链接 | 按补丁选择：仅 V3 M0 导航行 |
| `docs/README.md` | 修改 | V3 M0 文档索引条目 | Phase 6 审核发布索引条目 | 按补丁选择：仅 V3 M0 索引行 |
| `docs/ROADMAP.md` | 修改 | V3 M0 状态更新 | Phase 6 审核发布状态 | 按补丁选择：仅 V3 M0 状态行 |

#### 不纳入 V3 M0 提交（Phase 6 及其他）

| 文件 | 状态 | 归属 |
| --- | --- | --- |
| `docs/architecture/DEV-20260907-01-candidate-review-publish.md` | 新增 | Phase 6 审核发布 |
| `docs/decisions/BIZ-20260907-01-review-publish-authority-boundary.md` | 新增 | Phase 6 审核发布 |
| `docs/requirements/REQ-20260907-01-candidate-review-publish.md` | 新增 | Phase 6 审核发布 |
| `docs/progress/PROG-20260907.md` | 新增 | Phase 6 审核发布 |
| `docs/progress/PROG-20260908.md` | 新增 | Phase 6 审核发布 |

### 明确分开的三项授权

1. **设计批准**：用户明确批准五项设计决定（版本表、批准记录与九组校验、独立契约链、职责分工、M1–M6 顺序）。
2. **指定范围 Git 提交**：用户明确授权具体 M0 提交范围（T0 + V3 M0 文件），且实际提交完成。
3. **M1 开工**：用户明确授权 M1 实施。

三项授权独立，不得互相替代。未经授权不得执行对应动作。

### 批准后可执行的 M1 提示词

> 按 DEV §12 M1 与 §2 文件清单，实施 V3 下游 M1：
>
> **M1 允许的五个实现文件固定为：**
> 1. `src/release_sql_bot/domain/project_bindings_v3.py`（`ProjectBindingContextV3`/
>    `GovernedMetadataSnapshotV3`/`ApprovalRecordV3` 及 context/snapshot/approval 嵌套引用与 grant 模型；
>    `ApprovalClosureValidationErrorV3` 异常定义在同文件）
> 2. `src/release_sql_bot/domain/handoff_closure_v3.py`（`HandoffClosureV3`/
>    `RepositoryVerifiedHandoffV3`）
> 3. `src/release_sql_bot/application/validate_approval_closure_v3.py`（V3 专属纯函数；
>    V2 模块不得导入；无 wire schemaVersion；无外部副作用）
> 4. `tests/contract/test_project_bindings_v3_contract.py`
> 5. `tests/contract/test_handoff_closure_v3_contract.py`
> 不得使用仓库根目录下的 `application/` 路径。
>
> **禁止修改：** V2 模块、既有测试期望、配置、Schema、加载器常量、M2/M3 应用代码。
>
> **M1 范围：** 仅实现契约与九个有序检查组纯计算校验；不实现 MongoDB 仓储、生命周期事件存储或应用解析。
> `ResolveMetadataRequestV3` 的应用解析实现属于 M2，M1 不得进入。
>
> **`validate_approval_closure_v3` 纯函数（M1 核心）：**
> - 签名：`validate_approval_closure_v3(project_context, metadata_snapshot, approval_record) -> None`
> - 直接接受 `ProjectBindingContextV3`、`GovernedMetadataSnapshotV3`、`ApprovalRecordV3`；
>   不是跨版本共享函数，V2 模块不得调用或导入；可复用只有 canonical SHA-256 等底层算法；
> - 成功返回 `None`；任一九组检查失败时抛出 `ApprovalClosureValidationErrorV3`（仅含稳定中性 issue code）；
> - 不访问 MongoDB/provider/SQL Server/环境变量；不修改输入对象；
> - 验证逻辑即 DEV §5.2 所列九个有序检查组；
> - 实际载荷哈希使用 `application/canonical.py` 的 `canonical_content_sha256` 约定（camelCase、
>   JSON 模式、仅排除根级 contentSha256、不排除嵌套 sha256 和业务字段、不复用 handoff 特殊算法）；
> - 成功只证明内容闭包内部一致，不证明批准真实性，真实 M3/M6 必须通过独立受信批准来源核验（DEV §3.6）。
>
> **`ApprovalClosureValidationErrorV3` 异常：**
> - 定义在 `src/release_sql_bot/domain/project_bindings_v3.py`；
> - 只携带稳定 code（DEV §3.5 九个 issue code），不携带 ID/哈希实际值、私有对象或原始载荷。
>
> **`test_project_bindings_v3_contract.py` 最低覆盖：**
> - 九个有序检查组（DEV §5.2）各有一个精确反例；
> - 多重错误输入验证 fail-fast 顺序（按 §3.5 顺序返回第一个）；
> - 异常文本不包含测试输入中的 ID、哈希或载荷值；
> - V2 模块不导入 V3 纯函数（静态断言）；
> - 纯函数调用前后输入对象序列化结果完全一致；
> - 修改 context/snapshot 业务内容但保留旧自哈希和批准引用，必须失败；
> - 只修改 `context.authorizationPolicyVersion`，必须失败。
> 注意：`approval_id` 是不透明稳定 ID，**不是 SHA-256**。
>
> **`test_handoff_closure_v3_contract.py`：** 仍只测试 handoff 闭包，不混入 approvalRecord 测试。
>
> **测试：** 定向测试（2 文件）全部通过；全量 `uv run pytest` 通过数不低于 424。
>
> **Git 边界：** M1 完成后保持未提交供人工审查；commit 需要用户另行明确授权；push 需要用户另行
> 明确授权；M0 设计批准不授权数据库或在线模型调用。

## V3 M0 提交清单及引用闭包修复（2026-09-09 追加）

### 一、修正文件归属

`docs/progress/PROG-20260908.md` 是"V3 下游实施准备（来源核验 + 业务核查）"记录，
不是 Phase 6 文档。将其纳入 V3 拟提交范围。

Phase 6 三份 REQ/BIZ/DEV（`REQ-20260907-01`、`BIZ-20260907-01`、`DEV-20260907-01`）和
`PROG-20260907` 继续独立处理，不得仅为修复链接而把整套 Phase 6 内容塞入 V3 提交。

### 二、真实引用依赖

| 文件 | 依赖 | 说明 |
| --- | --- | --- |
| `docs/progress/PROG-20260909.md` | `docs/progress/PROG-20260908.md` | 前次进度引用 |
| `docs/bugs/BUG-20260908-02-v3-upstream-fact-catalog-contract-gaps.md` | `docs/progress/PROG-20260908.md` §三 | 关联进度 |
| `docs/progress/PROG-20260908.md` | `docs/progress/PROG-20260907.md` | 前次进度引用（Phase 6，跨范围） |
| `docs/bugs/BUG-20260908-01-v3-downstream-contract-source-hash-normalization.md` | `docs/progress/PROG-20260909.md` | 关联进度 |
| `docs/bugs/README.md` | `docs/bugs/BUG-20260908-01-*.md`、`docs/bugs/BUG-20260908-02-*.md`、`docs/bugs/BUG-20260901-01-*.md` | 索引 → 缺陷文件 |
| `docs/README.md` | 本次新增文档 | 索引 → 所有文档 |

**跨范围链接处理（PROG-20260908 → PROG-20260907）：**

在拟提交的 `PROG-20260908.md` 版本中，将指向未纳入提交的 `PROG-20260907` 链接改为带日期的普通文字历史说明：

```text
- 前次进度：2026-09-07（Phase 6 候选人工审核与发布方案规划，独立任务，另案提交）
```

保留真实历史，不伪造替代进度。只在提交预览中展示此补丁，本轮不覆盖工作区原文件。
Phase 6 以后独立提交时，可恢复该链接。

### 三、恢复历史 BUG 索引

从 HEAD 恢复被误删的 `BUG-20260901-01` 索引，保留原文：

```text
- [BUG-20260901-01：V2 在线候选 coverage 声明不稳定](BUG-20260901-01-v2-live-provider-coverage-declaration.md)——已修复；
  原始文件未进入公开历史，现版本为 2026-09-05 依据同期记录重建，见文档内"文档完整性说明"。
```

保留新增 BUG-01/BUG-02，避免重复。这是修复无关删除，不改变旧 BUG 状态。

### 四、混合文件精确补丁

#### README.md

| 类型 | 内容 | 处理 |
| --- | --- | --- |
| 纳入 | "2026-09-09 的实施规划已记录..."段落（已确认事实的 V3 口径） | ✅ 提交 |
| 纳入 | V3 M0 文档导航链接（REQ-20260906-04、BIZ-20260906-03、DEV-20260906-04） | ✅ 提交 |
| 排除 | Phase 6 审核发布导航链接（REQ-20260907-01、BIZ-20260907-01、DEV-20260907-01） | ❌ 保留工作区，不提交 |
| 排除 | "人工审核与发布（Phase 6）已完成方案规划..."段落 | ❌ 保留工作区，不提交 |
| 排除 | "当前进度"指向 `PROG-20260907` 的链接 | ❌ 保留工作区，不提交 |

#### docs/README.md

| 类型 | 内容 | 处理 |
| --- | --- | --- |
| 纳入 | V3 M0 文档索引条目（REQ-20260906-04、BIZ-20260906-03、DEV-20260906-04、BUG-20260908-01、BUG-20260908-02） | ✅ 提交 |
| 纳入 | "当前进度"指向已纳入的 `PROG-20260909` | ✅ 提交 |
| 排除 | Phase 6 审核发布索引条目（REQ-20260907-01、BIZ-20260907-01、DEV-20260907-01、PROG-20260907） | ❌ 保留工作区，不提交 |
| 排除 | "当前进度"指向被排除的 `PROG-20260907` 的链接 | ❌ 保留工作区，不提交 |

#### docs/ROADMAP.md

| 类型 | 内容 | 处理 |
| --- | --- | --- |
| 纳入 | V3 M0 状态更新（"2026-09-09 已完成上游 commit 与三类哈希来源登记..."） | ✅ 提交 |
| 排除 | Phase 6 状态更新（"已规划，未实施..."及 2026-09-07 规划登记段落） | ❌ 保留工作区，不提交 |

### 五、两次提交预览

#### 提交 1：T0 来源登记

| 文件 | 状态 | 内容 |
| --- | --- | --- |
| `docs/specs/fact-binding-request-3.0.0-source.json` | 修改 | verifiedCommitTree + runtime 三类哈希登记 |
| `tests/contract/test_fact_binding_v3_contract.py` | 修改 | 来源记录测试同步更新 |

**验证：** 来源 JSON 与测试成对纳入；无 Phase 6 内容；无历史内容删除。

#### 提交 2：V3 M0 核心规划

| 文件 | 状态 | 内容 | 补丁 |
| --- | --- | --- | --- |
| `docs/architecture/DEV-20260906-04-v3-downstream-pipeline-alignment.md` | 修改 | 核心设计文档 | 整文件 |
| `docs/decisions/BIZ-20260906-03-v3-downstream-authority-boundary.md` | 修改 | 权威边界决策 | 整文件 |
| `docs/requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md` | 修改 | 需求文档 | 整文件 |
| `docs/progress/PROG-20260909.md` | 新增 | 进度与审批准备 | 整文件 |
| `docs/progress/PROG-20260908.md` | 新增 | 来源核验与业务核查 | 补丁：PROG-20260907 链接改为文字说明 |
| `docs/bugs/BUG-20260908-01-v3-downstream-contract-source-hash-normalization.md` | 新增 | 来源登记缺陷 | 整文件 |
| `docs/bugs/BUG-20260908-02-v3-upstream-fact-catalog-contract-gaps.md` | 新增 | 业务表达缺口 | 整文件 |
| `docs/bugs/README.md` | 修改 | BUG 索引（含恢复的 BUG-20260901-01） | 整文件 |
| `README.md` | 修改 | 导航 + V3 口径 | 补丁：仅 V3 段落 |
| `docs/README.md` | 修改 | 文档索引 | 补丁：仅 V3 条目，当前进度指向 PROG-20260909 |
| `docs/ROADMAP.md` | 修改 | 路线图状态 | 补丁：仅 V3 状态行 |

**链接验证（M0 预览）：**

| 链接目标 | 来源 | 存在 |
| --- | --- | --- |
| `docs/progress/PROG-20260909.md` | `docs/README.md`、`README.md` | ✅ 纳入 |
| `docs/progress/PROG-20260908.md` | `PROG-20260909.md` | ✅ 纳入 |
| `docs/requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md` | 多个索引 | ✅ 纳入 |
| `docs/decisions/BIZ-20260906-03-v3-downstream-authority-boundary.md` | 多个索引 | ✅ 纳入 |
| `docs/architecture/DEV-20260906-04-v3-downstream-pipeline-alignment.md` | 多个索引 | ✅ 纳入 |
| `docs/bugs/BUG-20260908-01-v3-downstream-contract-source-hash-normalization.md` | `docs/bugs/README.md` | ✅ 纳入 |
| `docs/bugs/BUG-20260908-02-v3-upstream-fact-catalog-contract-gaps.md` | `docs/bugs/README.md` | ✅ 纳入 |
| `docs/bugs/BUG-20260901-01-v2-live-provider-coverage-declaration.md` | `docs/bugs/README.md` | ✅ 纳入 |

**未验证项：** Markdown 锚点（`#` 片段）未逐项验证；Phase 6 文件未纳入提交，
其跨范围链接已在 PROG-20260908 补丁中处理为文字说明。

### 六、用户需要授权的具体范围

1. **设计批准**：五项设计决定（版本表、批准记录与九组校验、独立契约链、职责分工、M1–M6 顺序）
2. **T0 提交**：`docs/specs/fact-binding-request-3.0.0-source.json` + `tests/contract/test_fact_binding_v3_contract.py`
3. **V3 M0 提交**：核心规划 3 份 + 进度 2 份 + 缺陷 3 份 + 混合文件精确补丁 3 份
4. **M1 开工**：五个实现文件

用户可在同一条消息中授予上述授权。本轮不代为批准，不提交，不进入 M1。
