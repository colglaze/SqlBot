# PROG-20260819：事实绑定契约切片

- 日期：2026-08-19
- 当前 Phase：Phase 2 完成
- 关联需求：[REQ-20260819-01](../requirements/REQ-20260819-01-fact-binding-intake.md)
- 关联设计：[DEV-20260819-01](../architecture/DEV-20260819-01-fact-binding-contract.md)
- 关联决策：[BIZ-20260819-01](../decisions/BIZ-20260819-01-agent2-role-alignment.md)

## 已完成

- 确认 SqlBot 是事实级 Agent 2，不再生成整规则异常集合 SQL；
- Python 运行时和项目约束固定为 `3.11.9`，应用版本升级为 `0.2.0`；
- 新增与 RuleReader Schema `2.0.0` 对齐的 camelCase `FactBindingRequest`；
- 新增 SQL Server 元数据上下文、事实绑定就绪结果和稳定阻塞原因码；
- 新增 `POST /api/v1/fact-bindings/validate`；
- 新增不可执行、待审核的 `SqlTemplateCandidate` 契约；
- 配置层固定 DeepSeek、SQL Server 和禁用临时表的基线，安全摘要不输出密钥；
- 更新 README、AGENTS、路线图和历史文档替代关系；
- 更新 `uv.lock` 并建立本地 `.venv`。

## 验证证据

- `uv run python --version`：`Python 3.11.9`；
- `uv run ruff check .`：通过；
- `uv run ruff format --check .`：51 个文件格式正确；
- `uv run pytest`：19 个测试全部通过；
- 测试覆盖正常接入、Schema `1.0.0`、`derived`、缺参数、缺元数据范围、参数来源/关系白名单、临时表门禁、候选不可执行状态和 HTTP API；
- 所有测试均未调用真实 DeepSeek、MongoDB 或目标 SQL Server。
- 跨仓库进程级互操作验证直接使用 RuleReader `valid_candidate_v2`、确定性校验和 `build_fact_binding_requests` 的实际输出；SqlBot 对 `task.settlement_fee` 返回 `ready`、`issues=[]`，未经过手工字段改写，且请求不含 `sourceExpression` SQL 片段。

## 当前结论

Agent 2 已能独立读取并判断 RuleReader 导出的事实绑定 JSON 是否具备生成条件；当前仍不会生成 SQL。`ready` 不是 SQL 生成成功或可执行的证明。

## 遗留与下一任务

下一任务需要新的 REQ/DEV：实现 DeepSeek provider 端口、版本化 Prompt、严格候选解析和离线模型替身。SQL AST、安全门禁、元数据采集、数据库连接、试跑和审核继续留在后续 Phase。
