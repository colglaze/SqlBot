# PROG-20260901：私有本地参考资料包整合

- 日期：2026-09-01
- 当前 Phase：Phase 2H 私有字段映射与来源资料包已完成；Phase 5 仍为 `待规划`
- 完成需求：
  [REQ-20260828-03](../requirements/REQ-20260828-03-local-candidate-evidence-integration.md)
- 受决策约束：
  [BIZ-20260828-03](../decisions/BIZ-20260828-03-local-candidate-evidence-boundary.md)
- 完成设计：
  [DEV-20260828-03](../architecture/DEV-20260828-03-local-candidate-evidence-package.md)
- 前置完成：Phase 2G、独立 V2 候选生成输入与 Phase 4 AST 静态安全门禁

## 本次完成

- 仅在用户指定目录盘点 17 个文件，没有扫描其他个人目录；
- 对文本、Office XML 和连接信息执行不回显匹配值的密钥模式检查，未命中；
- 建立独立私有 `colglaze/RuleDataReferences`，保存 17 个原始字节快照、派生候选证据、JSON Schema、
  manifest、治理说明和校验脚本；
- 使用 `.gitattributes` 禁止 `sources/**` 文本规范化，并验证 staged Git blob 的逐文件 SHA-256；
- 将派生证据中的本机绝对路径替换为仓库相对路径；
- 从 SqlBot 当前公开 `main` 移除完整候选 JSON、来源清单和 Schema，只保留固定 bundle 身份；
- 同步 RuleReader 公开进度中的脱敏来源登记，未触碰其 `.obsidian/workspace.json` 本地修改。

## 固定身份

- 私有 commit：`2240e5bd18e36d17650896a10cc61e1c18e3daa0`；
- Bundle digest：`6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`；
- 来源文件：17 个、433,997 字节；
- 工作簿 SHA-256：
  `872706f07792a618888a13d8944d7c59f454e5554388fa2dece511abebb30242`；
- 私有派生结果：197 条字段候选、93 条依赖候选、7 条来源候选。

## 公开历史纠正

- 并发出现的已推送提交 `63432ad7b601a941d4decb307de80d42168aa797` 曾把完整字段候选 JSON、
  来源清单和 Schema 放入公开 Git 历史；其中没有原始 Office 文件或完整视图 SQL，密钥模式检查未
  命中；
- 用户于 2026-09-01 单独明确授权重写 SqlBot `main` 并 force-push；
- `main` 已以安全基线 `acf04d0a72d70e4e5209c7b2eb6e38bbfa8dc517` 为父提交，用最终脱敏文件树
  生成单一新提交并通过 `--force-with-lease` 推送；`63432ad` 及临时清理提交 `de0139d` 均不再位于
  `refs/heads/main` 的可达历史；
- Git 是分布式系统，历史改写不能撤回第三方已完成的 clone、fork 或缓存；若需要平台级清缓存，必须
  另行联系 GitHub Support。本次没有发现需要轮换的凭据。

## 安全边界

- 当前公开 `main` 及其从安全基线开始的可达历史不包含原始 Office 文件、视图 SQL、规则原文、对象名/
  字段名列表、本机绝对路径或完整候选证据；
- 私有资料固定 `authority=none / executable=false`，不进入运行时；
- 未连接 SQL Server/MongoDB，未调用在线模型，未清除 blocking uncertainty，也未改变候选审批状态。

## 验证证据

- 私有 bundle 完整性、受管文件集合、密钥模式与内容摘要：通过；
- 私有派生 JSON Schema：通过；
- 私有 Python scripts Ruff、format、compileall：通过；
- SqlBot `py -m uv run ruff check .`：通过；
- SqlBot `py -m uv run ruff format --check .`：通过；
- SqlBot `py -m uv run pytest`：230 passed；
- SqlBot `git diff --check`：通过；
- 重写后远端 `main` 父链、禁止路径、个人绝对路径和全新 clone：通过；
- RuleReader Ruff、format、pytest 与 `git diff --check`：通过。

## 用户显式合成 SQL 雏形预览

- 用户于 2026-09-01 针对本次任务明确要求修改文档口径并查看生成 SQL 雏形；该授权只覆盖合成脱敏
  V2 输入的在线 provider 调用，不覆盖私有参考资料、MongoDB、SQL Server 或 SQL 执行；
- 首次有界生成的两次响应均被严格输出门禁拒绝；一次不保存、不回显原文的结构化诊断定位为
  `declaredUsageCoverage` 未与 stable `conditionId` 集合精确一致；
- Prompt 升级到 `sqlserver-fact-candidate-v2.1`，新增从同一权威输入确定性派生的
  `exactOutputDeclarations`，但不放宽应用交叉校验、不新增授权；
- 修复后的单次在线响应形成不可执行、待审核候选；其合成 SQL 雏形通过 Phase 4 SQLGlot AST 静态
  门禁，报告仍为 `passed / executable=false`；
- 全部在线调用累计 4 次；始终 `databaseAccess=false / sqlExecuted=false`，未启动服务、未读取私有
  bundle、未清除 blocking uncertainty，也未改变候选审批状态；
- 缺陷、修复与证据详见
  [BUG-20260901-01](../bugs/BUG-20260901-01-v2-live-provider-coverage-declaration.md)。

## 下一任务

Phase 5 受限 SQL Server 验证仍为 `待规划`。只有后续用户显式任务先建立独立 REQ/BIZ/DEV、确认最小
权限账号与环境授权后才可实施；本资料包不授权连接、prepare、explain 或执行任何候选 SQL。
