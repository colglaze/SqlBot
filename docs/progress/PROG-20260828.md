# PROG-20260828：Phase 2G 实现与后续安全链路

- 日期：2026-08-28
- 当前 Phase：Phase 2G、V2 候选生成输入对齐与 Phase 4 AST 静态安全门禁已完成
- 完成需求：[REQ-20260827-03](../requirements/REQ-20260827-03-project-context-metadata-resolution.md)
- 受决策约束：[BIZ-20260827-02](../decisions/BIZ-20260827-02-project-metadata-authorization-boundary.md)
- 完成设计：[DEV-20260827-03](../architecture/DEV-20260827-03-project-context-metadata-resolution.md)
- 前置完成：[REQ-20260827-02](../requirements/REQ-20260827-02-rulereader-fact-binding-v2-intake.md)
- 补充完成：[REQ-20260828-02](../requirements/REQ-20260828-02-rulereader-handoff-read-intake.md)
- 已完成顺序：独立 V2 候选生成输入对齐 → Phase 4 REQ/DEV V2 复核 → Phase 4 实施

## Phase 2G 已完成

- 新增独立、严格 camelCase、`extra=forbid` 的 `ProjectBindingContextV2 1.0.0`、
  `GovernedMetadataSnapshot 1.0.0`、`ResolveMetadataRequestV2 1.0.0` 与不可执行
  `BindingResolutionReportV2 1.0.0`；
- 统一 Phase 2F 与 Phase 2G 的 canonical JSON / SHA-256 算法；上下文和快照排除根级自身哈希字段后
  重算，篡改、错误引用、错误范围、未批准状态全部 fail closed；
- resolver 只做 V2 必需逻辑字段、上下文 relation/column/field/entity-key/join grant 与完整快照的
  确定性交集，不按名称相似、唯一候选、默认 schema 或最新版本推断；
- SQL Server logical type compatibility 固定为离线 `sqlserver-logical-type-compat-v1` 集合，不插入
  cast；大小写只服从快照明确声明的 `sensitive | insensitive`；
- 单关系无需 join；多关系必须同时命中快照 relationship edge 与上下文 join grant，并形成唯一树形
  闭包；缺失或歧义均阻断；
- source/mapping candidate 与 parser provenance 只输出 `consistent | conflict | notUsed` 处置及
  warning，不创建或扩大授权；
- 新增纯计算 `POST /api/v1/fact-bindings/v2/resolve-metadata`；200 仅表示确定性报告已形成，报告两个
  状态均固定 `executable=false`。

## 合成脱敏回归

- 新增无真实项目、表列或业务数据的 metadata-ready `FactBindingRequest 2.0.0` fixture；
- 覆盖批准单关系、批准双关系 join、大小写策略、候选一致/冲突、上游六类 blocking uncertainty、
  输入不变性与重复运行稳定性；
- 覆盖额外字段、snake_case、错误版本/类型、重复 ID、通配符、哈希篡改、错误 project/rule/request/
  snapshot 闭包、draft 状态、缺失/歧义/错误 role 授权、entity key 缺口、未知列、类型不兼容及 join
  缺口；
- 固定 candidate provider 与规则仓储替身在 `metadataResolved`、`blocked` 和 422 API 路径均无业务
  调用；应用不存在 SQL client 或执行端口。

## Phase 2G 验证证据

- `py -m uv run ruff check .`：通过；
- `py -m uv run ruff format --check .`：通过（格式修正后复核）；
- `py -m uv run pytest`：`122 passed`；
- 最终全链路完成后将再次运行上述权威检查与 `git diff --check` 并更新本记录。

## 安全边界

- 本次未读取本地参考工作簿；
- 未连接或执行 SQL Server，未读取 catalog，未装配 SQL client；
- 未调用 DeepSeek 或任何在线模型；
- 未清除、降级、补写或隐藏任何 blocking uncertainty；
- 未创建、持久化、批准、驳回或改变任何 SQL 候选状态。

## V2 候选生成输入对齐已完成

- [REQ-20260828-01](../requirements/REQ-20260828-01-v2-candidate-generation-input.md) /
  [BIZ-20260828-01](../decisions/BIZ-20260828-01-v2-candidate-authority-boundary.md) /
  [DEV-20260828-01](../architecture/DEV-20260828-01-v2-candidate-generation-input.md) 已建立并实现；
- `GenerateSqlCandidateRequestV2` 携带完整 Phase 2G request/report，应用在 provider 前重算并比较完整
  canonical report；blocking、伪造 report 和 context 篡改均零调用；
- `sqlserver-fact-candidate-v2` 只投影逻辑语义与授权物理计划，排除 source/mapping candidate、parser
  provenance、uncertainty reason、approval、examples、evidence IDs 和运行时参数实际值；
- `SqlTemplateCandidateV2 2.0.0` 的 lifecycle、refs、provenance 与 `contentSha256` 由应用组装，模型只
  提出 SQL 和不可信 declarations；
- API、严格输出、交叉引用、重试、错误隐藏和候选哈希均由固定 provider 合成回归覆盖；
- 未调用真实 DeepSeek/在线模型，未访问数据库或工作簿，未解析或执行 SQL；
- 完成时 `ruff check`、`ruff format --check`、`git diff --check` 通过，全量 `141 passed`。

## RuleReader 不可变事实交接只读接入已完成

- 新建 [REQ-20260828-02](../requirements/REQ-20260828-02-rulereader-handoff-read-intake.md) /
  [BIZ-20260828-02](../decisions/BIZ-20260828-02-rulereader-handoff-read-boundary.md) /
  [DEV-20260828-02](../architecture/DEV-20260828-02-rulereader-handoff-read-intake.md)，补齐 Phase 2F
  只接收 HTTP payload、尚未读取 RuleReader 不可变存储包装的缺口；
- 新增只读 `FactBindingHandoffRepository`，MongoDB 只按调用方显式给出的精确 `ruleVersion` 执行
  `find` 并按 `fact_code/_id` 排序；端口和适配器均没有 handoff 写方法、migration 或索引管理；
- Mongo 查询保留完整包装，使严格 `extra=forbid` 可以发现未知顶层字段；缺字段、额外字段、V1、
  跨版本、重复身份、包装/payload 身份冲突均使整批 fail closed；
- SqlBot 包内固定 RuleReader `FactBindingRequest 2.0.0` Draft 2020-12 Schema，内容逐行一致；按上游
  CRLF 字节校验来源 SHA-256
  `38fec6b22511984983e7e7fbbdb40afd58aeffd51b2de8ab73fdfb187024026b`；
- 每条记录同时经过独立 Pydantic consumer、固定 JSON Schema、来源闭包和 canonical payload hash
  门禁，再复用 Phase 2F gap analysis；批次输出按事实身份固定排序；
- `GET /api/v1/fact-binding-handoffs/v2?ruleVersion=<exact-version>` 只返回完整已验证批次；任一
  blocking request 使批次固定 `blocked/executable=false`，路由不读取或调用候选 provider；
- API 对 not found、无效上游记录和仓储不可用分别返回安全的 404/502/503，不回显 payload、MongoDB
  URI 或驱动异常；不接受 V1 降级，不生成或解析 SQL。

### 交接只读接入验证证据

- 定向离线回归：`48 passed`，覆盖 Schema/来源/hash、包装完整性、批次阻断、稳定排序、MongoDB
  精确只读查询、API 与 provider 零调用；
- `py -m uv run pytest -q`：`225 passed`；
- `py -m uv run ruff check .`：通过；
- `py -m uv run ruff format --check .`：`133 files already formatted`；
- `git diff --check`：通过，仅报告仓库既有 LF/CRLF 转换提示；
- 全部验证使用合成脱敏数据，未连接真实 MongoDB/SQL Server、未调用在线模型、未读取本地工作簿，
  未修改 RuleReader 或 `fact_binding_handoffs`。

## Phase 4 SQL AST 静态安全门禁已完成

- [REQ-20260827-01](../requirements/REQ-20260827-01-sql-ast-safety-gate.md) 与
  [DEV-20260827-01](../architecture/DEV-20260827-01-sql-ast-safety-gate.md) 已完成 V2 复核与实施；历史
  `FactBindingRequest 1.0.0 / SqlServerBindingContext / SqlServerValidationMetadata` 输入假设退出本
  阶段；
- 新增严格 `ValidateSqlCandidateRequestV2 1.0.0` 和不可变
  `SqlStaticValidationReportV2 1.0.0`；唯一输入为完整 `GenerateSqlCandidateRequestV2` 与
  `SqlTemplateCandidateV2 2.0.0`；
- parser 前固定重算 Phase 2G 完整报告、context/snapshot 内容哈希、generation input、候选自哈希及
  rule/request/project/fact/lifecycle 引用；任一篡改使 inspector 调用次数为零；
- SQLGlot 依赖固定 `>=30.17,<30.18`，锁文件精确解析为 `30.17.0`；具体 AST 只存在
  `infrastructure/sql/sqlglot_tsql.py`，domain/application 仅消费 parser-neutral DTO；
- adapter 使用完整 `tsql` parse 列表和固定复杂度上限，阻断空槽/多语句、DML/DDL/MERGE/EXEC、
  `SELECT INTO`、集合运算、hint、递归形状、临时/表变量、跨数据库、外部/函数型 source、未知函数、
  星号及错误 qualification；
- scope-aware inspection 区分 CTE/派生作用域和物理源；完整快照仅提供离线 schema，实际 relation、
  基础列及 join 类型/两端必须精确命中 Phase 2G 授权闭包。快照存在或单列分别获准都不能创建新权限；
- 仅接受位于 `WHERE | JOIN ON` 的 `:name`；AST 参数集合还与候选、事实参数及 V2 entity/filter/time
  逻辑交叉核对；
- 唯一显式 `fact_value` 投影必须追溯至授权结果基础列；只有所有前序门禁通过才按 stable
  `conditionId` 生成 coverage，且不声称证明 operator、`NOT`、空值或时间边界业务语义；
- 新增纯计算 `POST /api/v1/sql-candidates/v2/validate-static`；`passed | blocked` 均返回 200，wire
  错误 422，未知异常安全 500。入口不读取 repository/provider/catalog，不持久化，也不改变 candidate。

### Phase 4 合成脱敏回归与证据

- 真实 SQLGlot fixture 覆盖合法单表、CTE、授权 join、重复参数、`NOT`、空值、时间边界和大小写，
  以及空/多语句、DML/DDL/MERGE/EXEC/USE/SET/DECLARE/OUTPUT/INTO/set op/hint、临时/外部源、
  `@name/?/$1`、字符串/注释伪参数、星号、未知/歧义/未授权列、错误结果来源和复杂度边界；
- 应用与 API 回归覆盖候选/report/hash/reference 篡改的 inspector 零调用、声明差异、授权 join 不可重组、
  snapshot-only 列、结果/coverage、稳定报告、未知异常隐藏以及 provider 零调用；
- `py -m uv run pytest`：`230 passed`；
- `py -m uv run ruff check .`、`py -m uv run ruff format --check .` 与 `git diff --check`：通过；
- 全部数据为合成脱敏 fixture；未连接或执行 SQL Server，未调用在线模型，未读取本地参考工作簿，未
  清除或降级 blocking uncertainty，未修改候选 SQL、自身状态或审批状态。

## 下一任务

Phase 5 受限 SQL Server 验证仍为 `待规划`。只有后续用户显式任务先建立独立 REQ/BIZ/DEV、确认最小
权限账号与环境授权后才可实施；Phase 4 完成不授权连接、prepare、explain 或执行任何候选 SQL。
