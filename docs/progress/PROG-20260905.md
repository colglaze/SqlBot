# PROG-20260905：同步远程代码与本地验证

- 日期：2026-09-05
- 状态：`completed`（代码同步与离线验证）
- 当前 Phase：Phase 2G、V2 候选生成、Phase 4 静态安全门禁及 Phase 2H 参考资料整合已完成；Phase 5 已完成详细规划，待确认前置条件并实施 Phase 5A。
- 关联需求：[REQ-20260905-01](../requirements/REQ-20260905-01-restricted-sqlserver-validation.md)、[REQ-20260828-01](../requirements/REQ-20260828-01-v2-candidate-generation-input.md)、[REQ-20260827-01](../requirements/REQ-20260827-01-sql-ast-safety-gate.md)
- 关联设计/决策：[DEV-20260905-01](../architecture/DEV-20260905-01-restricted-sqlserver-validation.md)、[BIZ-20260905-01](../decisions/BIZ-20260905-01-restricted-sqlserver-validation-boundary.md)
- 前次进度：[PROG-20260901](PROG-20260901.md)

## 已完成

- 开始时本地 `main` 工作区干净，提交为 `37a659a`。
- 拉取 `origin` 后，以 `--ff-only` 将 `main` 更新至 `3d23162`，共同步 3 个提交，无合并冲突。
- SSH 对象传输停滞、HTTPS 直连失败后，使用系统现有代理完成同一远程仓库的 HTTPS 拉取；未修改持久 Git 远程地址或代理配置。
- 执行 `uv sync --locked`，本地项目由 `0.2.0` 更新至 `0.3.0`，依赖遵循远程锁文件。
- 修正两份技术文档中 Python 代码块的本地换行格式；未修改业务代码或代码块内容。

## 缺失文件调查与同日修复

- 针对下节遗留的 README 悬空引用，在用户授权下只读检索了 `pyWorkspace` 全部兄弟仓库
  （RuleAgent、RuleDataReferences 私有 bundle、RuleDataReferences-recovery）与 SqlBot 本地
  Git 对象、reflog、Codex 快照树；均未发现 `scripts/preview_synthetic_v2.py` 或原始
  BUG-20260901-01 文档，本机 `.codex_tmp` 预览产物也已不存在。
- 通过 GitHub API 按 SHA 探测并经临时克隆核验被改写的旧提交 `63432ad` 与 `de0139d`
  （对象尚未被远端 GC）：两棵文件树均不含这两份文件，且 `de0139d` 是 `63432ad` 的直接子提交；
  `git log -S` 显示 README 的预览脚本引用由当前 tip `3d23162` 引入。结论：两份文件从未进入
  任何公开提交，原始工作区已被清理，无法补齐原件。
- 按既定兜底方案：以 PROG-20260901 同期记录和现有代码事实重建
  `docs/bugs/BUG-20260901-01-v2-live-provider-coverage-declaration.md`，并在文档头部显式标注
  重建来源，未虚构复现输入或在线验证证据。
- README“查看 SQL 雏形的安全口径”移除不可运行的预览脚本命令，改为基于
  `/api/v1/sql-candidates/v2/generate` + `/validate-static` 的说明并链接 BUG 文档；
  `docs/bugs/README.md` 登记该缺陷；根 README“当前进度”链接更新到本文档。
- `.gitattributes` 追加 `*.md text eol=lf`，固定 Markdown 换行，避免 Windows checkout 再次
  触发相同格式差异。

## 预览脚本重新实现（同日补充）

- 经用户确认后，按当前 0.3.0 契约重新实现 `scripts/preview_synthetic_v2.py`（非原件恢复）：
  默认离线自测模式复用与离线回归同源的合成夹具（`tests/phase2g_support.py`），经固定离线
  provider 完整走一次 `generate_sql_candidate_v2`（Phase 2G 重算、`sqlserver-fact-candidate-v2.1`
  Prompt、严格输出门禁），再用真实 `SqlglotTsqlInspector` 在本地运行 Phase 4 门禁；
- `--live` 才会按配置调用在线 DeepSeek provider（有界重试，未配置时明确退出并提示），并对四类
  生成失败输出清晰中文错误与独立退出码；
- 产物写入被 Git 忽略的 `.codex_tmp/v2-candidate-preview.json` 与 `v2-static-report.json`，
  可用 `--output-dir` 覆盖；
- 新增 `tests/contract/test_preview_synthetic_v2_script.py`：离线路径产出的候选固定
  `candidate / executable=false / pending`，真实门禁 `passed`，usage coverage 命中
  `amount-positive`；
- 2026-09-05 用户配置 DeepSeek 并明确授权后完成首次 `--live` 在线预览：前两次模型响应被严格
  输出门禁拒绝（`attemptCount=3`），第三次形成有效候选，本地 Phase 4 门禁 `passed（issues=0）`；
  当日在线 HTTP 调用累计 6 次（1 次 401 失败尝试、2 次掩码诊断 ping、3 次生成尝试），始终
  `databaseAccess=false / sqlExecuted=false`，候选固定不可执行。

## Phase 5 规划（同日补充）

- 新增 `REQ-20260905-01`、`BIZ-20260905-01` 和 `DEV-20260905-01`，将 Phase 5 从“待规划”推进为
  “已规划，待确认前置条件并实施”；三份文档状态均为 `proposed`，不表示用户已批准连接数据库。
- Phase 5 拆为 5A 描述验证、5B 估算计划、5C 有界非生产试跑；首个实现切片只交付 5A。
- 输入必须携带完整 V2 generation/candidate/static report，并在数据库连接前重算 Phase 2G 和 Phase 4；
  任一篡改、不一致、blocking uncertainty 或不安全 profile 都必须保持 adapter 调用次数为 0。
- 首版选择本地 CLI，避免在当前没有调用方认证、授权和速率限制的 FastAPI 上增加数据库访问入口。
- 计划定义独立 validation switch、非生产硬门禁、TLS、最小权限证明、批准快照漂移检查、token-aware
  参数 binder、结果描述、SHOWPLAN、安全 XML、资源上限、取消、结果脱敏和不可变审计边界。
- 参数、SQL、结果、目标、凭据和原始执行计划不得进入普通日志或公开报告；所有阶段报告继续固定
  `executable=false`，人工审核和发布仍属于 Phase 6。
- 飞书与固定私有 bundle 仅作为非权威参考；计划没有复制内部对象、字段、SQL 或业务数据，也不以
  参考资料创建 snapshot、grant、参数值或数据库授权。

## 验证证据

- `uv run python --version`：Python `3.11.9`。
- `uv sync --locked`：通过，锁文件未修改。
- `uv run ruff check .`：通过。
- `uv run ruff format --check .`：首次发现两份 Markdown 代码块使用 CRLF，与配置要求的 LF 不一致；格式化后复查通过，`139 files already formatted`。
- `uv run pytest`：`230 passed`。
- `uv run release-sql-bot check-config`：通过，数据库能力关闭，MongoDB、SQL Server 和 DeepSeek 均未配置。
- `git diff --check`：通过；`HEAD...origin/main` 双向差异为 `0 / 0`。本地进度记录、索引和换行调整未提交。
- 初始代码同步与首次离线验证阶段未启动服务、未连接业务数据库、未调用在线模型、未执行 SQL，也未推送远程。
- 同日修复后复核：`uv run ruff check .` 通过；`uv run ruff format --check .` 通过
  （`140 files already formatted`，含重建的 BUG 文档）；`uv run pytest` `230 passed`；
  `git diff --check` 通过。
- 预览脚本离线实跑：门禁 `passed（issues=0）`、Prompt `sqlserver-fact-candidate-v2.1`、
  `content_sha256=c134b494…330c`。
- 在线预览实跑（用户当次授权）：门禁 `passed（issues=0）`，Prompt `sqlserver-fact-candidate-v2.1`，
  候选 `content_sha256=7c3e4f79…1b57`，`attemptCount=3`；全量 `uv run pytest` `234 passed`
  （含 4 项脚本契约测试）；`uv run ruff check .` 与 `uv run ruff format --check .` 通过。
- Phase 5 规划完成后：`uv run ruff check .` 通过；`uv run ruff format --check .` 通过
  （`146 files already formatted`）；`uv run pytest` `234 passed`；`git diff --check` 与新增/修改文档的
  本地 Markdown 链接检查通过。规划期间未连接 SQL Server/MongoDB、未调用在线模型、未执行 SQL。

## 遗留问题与下一步

- 在线预览已完成一次（用户当次授权）；后续每次在线调用仍需用户针对当次任务的明确授权。
- BUG-20260901-01 原始文档仍为重建件；若用户在其他机器找到 2026-09-01 的原始文档，可用原件
  替换并在 provenance 中记录替换。
- GitHub 远端对象库中旧提交 `63432ad` / `de0139d`（含已泄漏证据）按 SHA 仍可达；公开 `main`
  可达历史已不含它们。如需平台级彻底清除，须另行联系 GitHub Support（与 PROG-20260901 记录一致）。
- Phase 5 已新增 proposed 需求、边界决策和技术实施计划：
  [REQ-20260905-01](../requirements/REQ-20260905-01-restricted-sqlserver-validation.md)、
  [BIZ-20260905-01](../decisions/BIZ-20260905-01-restricted-sqlserver-validation-boundary.md)、
  [DEV-20260905-01](../architecture/DEV-20260905-01-restricted-sqlserver-validation.md)。
- 计划将 Phase 5 拆为 5A 描述验证、5B 估算计划和 5C 有界非生产试跑；首个实现只交付 5A 本地 CLI，
  不在当前无鉴权 FastAPI 上新增数据库执行入口。
- Phase 5A 实现前仍需确认非生产 target profile、owner、SQL Server 版本、认证方式、最小权限账号、
  证书信任链和参数数据分类；缺少任一项时只允许离线契约开发。
- 飞书与私有 bundle 继续固定为非权威参考，不创建 snapshot、grant、参数或连接/执行许可。

## 2026-09-06 复核说明

本节为 2026-09-06 进度复查的追加记录，只补充证据与澄清，不修改上文任何原始事实。

### 1. `722f817` 历史快照重新运行

以 `git worktree` 在被 Git 忽略的独立目录检出 `722f817`（当前工作区未受任何影响，取证后已清理），
在快照内重新运行全部权威检查：

- `uv run python --version`：Python `3.11.9`；
- `uv sync --locked`、`uv run ruff check .`：通过；
- `uv run ruff format --check .`：`143 files already formatted`；
- `uv run pytest`：`234 passed`；
- 离线预览（`python -m scripts.preview_synthetic_v2` 默认离线模式）实际运行：candidate
  `contentSha256 = c134b494b8c8…1e330c`，与上文记录的 `c134b494…330c` 逐字符一致；
  Prompt `sqlserver-fact-candidate-v2.1`；静态门禁 `passed（issues=0）`；候选固定
  `executable=false / reviewStatus=pending`。

### 2. 现存 2026-09-05 在线预览产物重新验证

对 `.codex_tmp` 中当日 `--live` 运行留下的 `v2-candidate-preview.json` 与
`v2-static-report.json`（Git 忽略的本地证据）做只读重验：

- candidate 内容哈希按 canonical 算法重算，与载荷 `contentSha256 = 7c3e4f79…1b57` 一致；
- `provenance`：provider `deepseek`、model `deepseek-v4-flash`、prompt
  `sqlserver-fact-candidate-v2.1`、`attemptCount=3`；
- 候选状态仍为 `candidate / executable=false / reviewStatus=pending`；
- 静态报告 `passed`、`issues=0`、`executable=false`；
- 静态报告 `candidateRef.candidateContentSha256` 与 candidate 内容哈希一致。

### 3. 审计限制

- 本地产物只能证明最终候选内容与该逻辑生成运行的 `attemptCount=3`；
- 上文"当日在线 HTTP 调用累计 6 次（1 次 401 失败尝试、2 次掩码诊断 ping、3 次生成尝试）"
  来自当日 PROG 记录，是对当时会话的叙述；
- 当前没有独立脱敏调用审计文件可以重新证明该完整调用序列；该缺口如实记录，不补写、
  不重构任何调用证据。

### 4. Phase 5 计划文档状态变化说明

[REQ-20260905-01](../requirements/REQ-20260905-01-restricted-sqlserver-validation.md)、
[BIZ-20260905-01](../decisions/BIZ-20260905-01-restricted-sqlserver-validation-boundary.md)、
[DEV-20260905-01](../architecture/DEV-20260905-01-restricted-sqlserver-validation.md) 在
2026-09-05 规划完成时状态均为 `proposed`；2026-09-06 经用户当次授权实施 Phase 5A 后分别更新为
`approved` / `in_progress`。这是"规划 → 实施"的时间演进，不是历史记录冲突；上文
"三份文档状态均为 proposed" 描述的是当时的准确事实，按原文保留。
