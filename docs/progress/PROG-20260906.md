# PROG-20260906：Phase 5A 受限 SQL Server 描述验证实现

- 日期：2026-09-06
- 状态：`completed`（Phase 5A 离线实现与验收）
- 当前 Phase：Phase 2G、V2 候选生成、Phase 4 静态安全门禁、Phase 2H 参考资料整合与 Phase 5A
  describe-only 验证已完成；Phase 5B、5C、持久化审计集合、HTTP 入口与 Phase 6 未实施。
- 关联需求：[REQ-20260905-01](../requirements/REQ-20260905-01-restricted-sqlserver-validation.md)
- 关联设计/决策：[DEV-20260905-01](../architecture/DEV-20260905-01-restricted-sqlserver-validation.md)、
  [BIZ-20260905-01](../decisions/BIZ-20260905-01-restricted-sqlserver-validation-boundary.md)
- 前次进度：[PROG-20260905](PROG-20260905.md)

## 已完成

- 实施前确认 2026-09-05 的三份 proposed Phase 5 文档：按用户当次任务授权将 REQ 置为
  `in_progress`、BIZ 置为 `approved`、DEV 置为 `in_progress`，并补充 Milestone 0–5 交付记录；
  Phase 5B/5C/Phase 6 保持未实施。
- 新增依赖 `pyodbc>=5.2,<5.3`（`uv add` 经系统代理完成解析并更新 `uv.lock`）。
- 领域契约 `domain/sqlserver_validation.py`：`ValidateSqlServerRequestV2`、`ValidationCaseV2`、
  `ParameterBindingV2`、`SqlServerValidationIssueV2`、`TargetEvidenceV2`、`PermissionEvidenceV2`、
  `SnapshotDriftEvidenceV2`、`DescribeEvidenceV2`、`BoundSqlRefV2`、`SqlServerValidationReportV2`；
  camelCase wire、`extra=forbid`、拒绝嵌套 snake_case、`status` 固定 `passed|blocked|inconclusive`、
  报告固定 `executable=false`、canonical `reportSha256`（排除自身哈希字段）；issue 按
  `stageOrder+code+fieldPath+safeIdentifier` 稳定排序；报告不含 SQL、参数值、连接目标、凭据、
  原始异常或业务结果；新增 `sqlserver-type-policy-v1` 类型解析/兼容矩阵。
- 端口 `application/ports/sqlserver_validation.py`（open/apply_policy/attest target/attest
  permissions/catalog facts/describe/rollback/close，driver-neutral 错误类别）与
  `application/ports/sql_parameter_binding.py`。
- 参数 binder `infrastructure/sql/sqlserver_parameter_binder.py`（`sqlserver-token-binder-v1`）：
  基于 SQLGlot tokenizer 的 COLON+VAR token span 从后向前替换；`:name` → `@pN`（describe）与
  `?`（qmark，为 5C 预留、本轮不执行）；字符串/注释/括号标识符/`::` 中的冒号不替换；重复参数
  稳定排序；记录原 SQL/派生 SQL SHA-256；无法唯一解释的占位符 fail closed。
- 配置 `config/settings.py` 与 `.env.example`：`RSB_SQLSERVER_VALIDATION_ENABLED`（默认 false，
  独立于 `RSB_DATABASE_ENABLED`）、profile ID、environment class、allowed modes（仅
  `describeOnly`）、最大并发、LOCK_TIMEOUT、describe 行/字节上限、参数 HMAC key、支持的主版本；
  启用时配置阶段硬拒绝 production（服务环境与 profile 环境）、未加密、信任服务器证书、缺失
  HMAC key（>=16 字符）、目标/凭据/profile 不完整；`safe_summary()` 只新增布尔值、profile ID、
  环境分类、允许模式和数值上限。
- 应用编排 `application/sqlserver_validation.py`：数据库访问前完整重算 Phase 2G（复用
  `resolve_metadata_v2` 经 Phase 4 服务）与 Phase 4（复用 `validate_sql_candidate_v2`，未复制规则）、
  canonical 比对携带 static report、校验 candidate 生命周期与 blocking uncertainty、profile/mode/
  requestedLimits/参数集与值策略；任何前置失败保持 adapter 调用次数为 0（测试断言）；数据库阶段
  依次 session policy → 目标身份 → 权限 → catalog 漂移 → describe，所有异常路径 rollback+close 并
  映射稳定 issue；不可用/超时 → `inconclusive`，确定性门禁失败 → `blocked`。
- ODBC adapter `infrastructure/sqlserver/odbc.py`：连接串仅由结构化字段构建（Encrypt=yes、
  TrustServerCertificate=no、ApplicationIntent=ReadOnly、Pooling=no、MARS_Connection=no、固定
  Application Name；拒绝 `;{}0x00` 值并自检未知/重复关键字）；固定 session policy 语句
  （SET NOCOUNT ON / XACT_ABORT ON / LOCK_TIMEOUT / TRANSACTION ISOLATION LEVEL READ COMMITTED）
  及 `@@LOCK_TIMEOUT` 回读；目标身份经 domain-separated HMAC 指纹比对；对象 SELECT/
  DML/DDL/CONTROL/TAKE OWNERSHIP、IMPERSONATE、db_owner/db_datawriter/sysadmin 参数化探测；
  仅对 Phase 4 实际对象做 catalog 漂移探测（alias/CLR 类型、缺列、类型/精度/空值性漂移阻断；
  额外对象/列只告警不扩大授权）；`sys.sp_describe_first_result_set` 三个参数全部驱动绑定；
  驱动错误按 SQLSTATE 映射为稳定错误类别，不透传原始文本。
- CLI `__main__.py` 改为 subparser：`serve`、`check-config`、`validate-sqlserver --input/--output
  [--mode describeOnly][--overwrite]`；不提供 `--sql/--host/--database/--username/--password`；
  开关关闭或 wire 错误退出码 4；报告独占创建、`--overwrite` 才可覆盖；stdout 仅输出 run ID、
  mode、status、issue codes、耗时和报告路径；不启动 FastAPI、不调用模型、不读私有参考 bundle。
- pyodbc 能力核对（离线证据）：`pyodbc 5.2.0` 提供 Python 3.11 wheel 并安装成功；随包
  `pyodbc.pyi` 证实 `Cursor.timeout`（查询超时秒数）、`Cursor.cancel()`、`connect(str, timeout=)`
  登录超时、`paramstyle="qmark"`、`Connection.rollback/close`。
- 文档同步：README（能力、配置表、Phase 5A 用法与安全线）、docs/README.md 索引、ROADMAP
  Phase 5 状态、REQ/BIZ/DEV 状态与实现记录、本 PROG。

## 测试

新增 134 项测试，全部离线（固定替身 + fake DB-API，无网络/Docker/真实数据库/在线模型）：

- `tests/contract/test_sqlserver_validation_contract.py`（23）：camelCase、extra=forbid、snake_case
  拒绝、重复参数绑定、结构化值拒绝、报告 `executable=false` 固定、issue 排序、type policy 矩阵。
- `tests/unit/test_sqlserver_parameter_binder.py`（12）：单/多/重复参数、字符串/注释/标识符伪参数、
  `::`、尾随冒号 fail closed、哈希稳定、qmark 形式。
- `tests/unit/test_sqlserver_validation.py`（55）：happy path 单周期探测、报告 canonical 哈希复算、
  requestedLimits 收紧、全部前置篡改（携带报告/候选 SQL/自哈希/伪造 passed/blocking uncertainty）
  adapter 调用次数为 0、profile disabled/不存在/production/TLS/mode/放宽限制、参数缺失/额外/
  类型不一致/list/NaN/Infinity、binder fail closed、目标身份/版本、SELECT 缺失、写入能力、
  高角色、权限不可判定、对象/列缺失、类型/空值性/对象类型漂移、modify 告警、额外列/对象不扩大
  授权、catalog 不可判定、描述 0 列/多列/错别名/错类型/可空性、describe 拒绝、行数上限、超时
  inconclusive、连接失败、驱动不可用、泄漏（报告 JSON 无 SQL/列名/对象名/目标/用户名/参数值）、
  状态与生命周期不变量。
- `tests/unit/test_sqlserver_odbc.py`（23）：连接串固定关键字/Windows 认证/注入拒绝/不安全 TLS
  拒绝、session policy 语句顺序与回读、目标指纹、权限与 catalog 探测参数化、缺对象不误报、
  describe 三参数绑定、SQLSTATE 错误映射、异常后 cursor/连接清理、行/字节上限。
- `tests/unit/test_sqlserver_validation_settings.py`（13）：默认关闭、各不安全配置组合在配置阶段
  拒绝、safe_summary 不含目标/凭据/HMAC key、与 MongoDB 开关独立。
- `tests/integration/test_sqlserver_validation_cli.py`（8）：端到端 happy path（退出码 0、报告落盘、
  stdout 白名单字段且无敏感内容）、泄漏、覆盖保护、blocked 退出码 2、开关关闭/坏 JSON/缺文件
  退出码 4、不启动服务不调用 provider。

## 验证证据

- `uv run python --version`：Python `3.11.9`。
- `uv add "pyodbc>=5.2,<5.3"`：解析成功，`uv.lock` 已更新（经系统代理；直连 pypi TLS 失败）。
- `uv sync --locked`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run pytest`、
  `git diff --check`、`uv run release-sql-bot check-config`：最终复核全部通过（见文末复核记录）。
- `uv run pytest`：`368 passed`（原 234 + 新增 134）。
- `check-config`：SQL Server 验证开关关闭、HMAC key 未配置、MongoDB/SQL Server/DeepSeek 摘要
  正常且不含敏感值。
- 本次未连接 SQL Server/MongoDB、未执行候选 SQL、未调用在线模型、未读取飞书或私有参考 bundle、
  未推送远程。

## 遗留问题与下一步

- 真实隔离 SQL Server 冒烟（DoD 第 10 条）未执行：需用户确认非生产 target profile、owner、
  SQL Server 版本、认证方式、最小权限账号和证书信任链后另行授权；不影响本轮其余 DoD。
- 目标身份校验要求 profile host 与实时 `SERVERPROPERTY('ServerName')`、`DB_NAME()` 的 HMAC 指纹
  一致；配置 profile 时必须使用与实际实例一致的名称（命名实例场景需按 SERVERPROPERTY 值填写），
  否则运行会确定性 blocked。
- 服务器身份指纹与参数值指纹共用配置 HMAC key，但使用不同 domain 前缀（`identity:v1:` /
  `param:v1:`）实现密钥分离；若需独立轮换可在后续任务拆分为两个 secret。
- 下一步（需独立任务与授权）：Phase 5B 估算计划（先做 SHOWPLAN 参数化 spike）、Phase 5C 有界
  试跑、不可变审计持久化（独立 migration）、HTTP 入口的认证授权设计。当前配置仅允许
  `describeOnly`，上述能力均未实现。

## 文末复核记录（同日补充）

最终验证命令结果：

- `uv run python --version` → `Python 3.11.9`
- `uv sync --locked` → 通过，锁文件与 pyproject 一致
- `uv run ruff check .` → `All checks passed!`
- `uv run ruff format --check .` → `161 files already formatted`
- `uv run pytest` → `368 passed`
- `git diff --check` → 通过（无 whitespace 错误）
- `uv run release-sql-bot check-config` → 通过，`sqlserver_validation_enabled=false`

## 下一阶段规划：真实上游交接与端到端候选证据闭环（同日补充）

- 用户要求编写下一阶段详细计划书，解决真实 Agent 1 规则交接缺失、受治理元数据输入缺失，以及
  真实 V2 SQL 候选无法形成端到端审计闭环的问题。本节任务只新增/更新文档，未实现任何代码。
- 新增三份 `proposed` 文档并互相引用：
  [REQ-20260906-02](../requirements/REQ-20260906-02-real-upstream-handoff-evidence-loop.md)、
  [BIZ-20260906-01](../decisions/BIZ-20260906-01-agent1-metadata-review-sqlbot-boundary.md)、
  [DEV-20260906-02](../architecture/DEV-20260906-02-real-handoff-evidence-loop-orchestration.md)；
  同步更新 README、docs/README 索引与 ROADMAP（新增 Phase 4R 小节，并使其成为进入 Phase 5B/5C
  的前置门禁）。
- 计划要点：优先于 Phase 5B/5C 与 Phase 6 的七条理由（无 rule_versions、无
  fact_binding_handoffs、V3 记录仍 blocked、无真实请求即无真实候选、无批准上下文/快照即无法进入
  生成、无 Phase 4 真实候选即无 Phase 5B 验证对象、候选持久化不能替代上游闭包）；阶段状态机
  `blockedUpstream → readyForMetadataResolution → metadataResolved → candidateGenerated →
  candidateStored → staticPassed → evidenceComplete`；里程碑 M0–M8（基线审计 → Agent 1 就绪 →
  上下文/快照审批 → 真实 intake 与 Phase 2G → 单事实在线生成（另行授权）→ insert-only 持久化 →
  Phase 4 门禁 → 证据包 → Phase 5A 冒烟决策门）；19 个稳定 issue code；24 类离线测试场景；
  12 项开放问题并标注各自阻断的里程碑。
- 关键事实登记（观察时间 2026-09-06，来源标注于 REQ 第 1.1 节）：MongoDB 只读检查结果（用户提供，
  本任务未连接验证）为 `rule_versions=0`、`fact_binding_handoffs=0`、
  `rule_structure_candidates_v3=1` 条（`validatedBlockedCandidate`、16 blocking）；只读参考 Agent 1
  仓库（未修改）：其 `BIZ-20260905-02` 已批准路径 B（V3 冻结 `FactBindingRequest 3.0.0`、禁止
  静默降级为 2.0.0），其 `PROG-20260906` 记录 18 条请求离线就绪但未落库——因此 MongoDB 中那条
  blocked 记录是确认前的陈旧候选，不是当前规则版本。契约版本决策（2.0.0 直接可用或 SqlBot
  intake 升级 3.0.0）登记为 Milestone 0 的阻断性前置。
- 候选持久化实现已提交为 255196f（本节撰写时尚未提交，原"仍在工作区未提交"表述已过时，状态
  收敛见文末『候选持久化交付状态收敛』）：DEV-20260906-02 第 6 节将其列为进入真实存储验证的
  10 项前置验收（含代码审查、386 项离线基线、insert-only、唯一索引、幂等、失败语义、PROG 证据、
  独立 Mongo integration、账号隔离）。
- 判定：未发现需要登记 BUG 的“现有实现与已批准文档冲突”；候选持久化的实现/证据滞后属于
  approved 需求尚未完成登记，已按前置验收项处理。
- 验证证据：`uv run ruff check .` 通过；`uv run pytest` 复跑 `386 passed`（首次全量运行曾出现
  5 个 `tests/contract/test_preview_synthetic_v2_script.py` 顺序依赖失败，单独运行与全量复跑均
  通过，属未提交测试代码的既有不稳定，本任务未修改测试）；`git diff --check` 通过；本地
  Markdown 链接检查通过（376 个相对链接全部有效）。`uv run ruff format --check .` 对本任务新增/
  修改的全部 Markdown 文件通过；当前唯一不合格项是用户在规划期间并行修改的
  `tests/contract/test_preview_synthetic_v2_script.py`（会话开始时的基线快照中该文件为干净状态，
  规划期间出现 +146 行未提交修改，属用户在途改动，本任务按约定未触碰，也未格式化）。
- 本节任务未连接 MongoDB/SQL Server、未调用 DeepSeek、未执行 SQL、未读取私有 bundle、未提交
  或推送。

## 候选存储前置验收审查（同日补充，用户选定任务）

- 用户在规划完成后选定先执行 [DEV-20260906-02](../architecture/DEV-20260906-02-real-handoff-evidence-loop-orchestration.md)
  第 6 节的 10 项前置验收。任务边界：只读审查代码与测试、运行离线测试、登记证据；未修改任何
  应用代码或测试，未连接 MongoDB/SQL Server，未调用模型。
- 逐项结论（详表见 DEV-20260906-02 第 6.1 节）：
  - 通过（7 项）：契约/代码审查对照 DEV-20260906-01 逐条一致；全量离线测试；insert-only
    （全 database 适配层扫描仅 `insert_one` 与 `create_index`，无 update/replace/delete/bulk/drop）；
    `contentSha256` 唯一索引；同 hash 幂等（duplicate 不报错不重复落库）；失败语义（ping/建索引
    失败 → unavailable、insert 异常 → failed、均不抛出、生成失败时 store 零调用）；生命周期
    （lifespan 按开关 initialize/close，与 `build_database_resources` 装配一致，不依赖
    `RSB_DATABASE_ENABLED`）。
  - 低严重度发现（1 项，已解决）：`mongodb_candidates.py` 的 `initialize` 成功日志输出
    database/collection 名称，超出 DEV-20260906-01 第 5 节日志白名单字面范围；库名/集合名为
    README 公开默认配置，无敏感泄漏、无可复现缺陷，不构成 BUG 登记，列为实现提交时的修订项
    （收敛日志或修订白名单表述，二选一）。已按"收敛日志"选项解决，见文末
    『候选持久化交付状态收敛』。
  - 保持开放（3 项，其中第 1 项已于同日完成，见文末『候选持久化交付状态收敛』）：实现提交与
    REQ/DEV-20260906-01 完成登记（已完成：实现为提交 255196f，登记为本次收敛任务）；独立 Mongo
    integration 测试无占位需显式授权补齐；存储账号与 RuleReader 只读账号的实际权限隔离需运维
    在真实部署前确认。后两项仍开放，均不影响第 1–7 项结论。
- 验证证据：`uv run pytest tests/unit/test_candidate_store.py` → `12 passed`；
  `uv run pytest` → `386 passed`；`uv run ruff check .` 通过；376 个 Markdown 相对链接检查通过。
- 本任务未提交、未推送、未执行任何外部写操作。

## 候选持久化交付状态收敛（同日补充）

- 任务边界：修复"实现已提交（255196f）但 REQ/DEV 仍为 `approved`、前文仍表述为实现未提交"的
  状态漂移，并收敛已登记的日志白名单偏差；未修改 candidate 响应契约、集合文档形状、唯一索引、
  幂等语义或失败降级行为，未新增任何 update/replace/delete 路径。
- 代码变更：`infrastructure/database/mongodb_candidates.py` 的 `initialize` 成功日志由输出
  database/collection 名称改为固定安全文案（不含库名、集合名、URI、SQL、凭据），即前置验收给出
  的"收敛日志"选项；模块 docstring 同步声明不记录存储定位信息。
- 测试变更：`tests/unit/test_candidate_store.py` 新增 5 项日志卫生测试，覆盖 initialize 成功、
  initialize 失败、落库成功、幂等 duplicate、写入失败五类日志，断言均不含数据库名、集合名、
  URI、凭据、候选 SQL 文本与驱动错误细节；测试使用非默认库名/集合名，任何定位信息插值都会使
  断言失败。
- 文档变更：[REQ-20260906-01](../requirements/REQ-20260906-01-v2-candidate-persistence.md) 与
  [DEV-20260906-01](../architecture/DEV-20260906-01-v2-candidate-persistence.md) 状态由
  `approved` 改为 `completed`，分别新增实施与验收记录节并登记实现提交 255196f；DEV 第 5 节
  日志白名单同步明确禁止数据库/集合名称；本 PROG 前文两处过时的"实现未提交/待独立任务"表述
  已更正并指向本节。README、docs/README 索引与 ROADMAP 经核对无过时表述，未修改。
- 验证证据：`uv run pytest tests/unit/test_candidate_store.py` → `17 passed`；`uv run pytest` →
  `391 passed`；`uv run ruff check .` → `All checks passed!`；`uv run ruff format --check .` →
  `169 files already formatted`；`git diff --check` 通过；本次修改文档内的相对链接全部有效。
- 明确保留的开放项（均未完成，不得视为已完成）：真实 MongoDB integration 测试未执行；实际部署
  账号对 `release_sql_bot` 库的建集合/写权限未验证；本次及此前全部验证均未连接真实 MongoDB；
  持久化不代表审批或可执行，候选仍固定 `candidate / executable=false / reviewStatus=pending`；
  DEV-20260906-02 第 6 节剩余前置验收（独立 Mongo integration、账号隔离运维确认）保持开放。
- 本任务未连接 MongoDB/SQL Server、未调用 DeepSeek、未执行 SQL、未读取 `.env`、未修改三个
  未跟踪的 Phase 4R 文档、未推送远程。

## FactBindingRequest 3.0.0 intake 契约审计与规划（同日补充）

- 任务边界：为 SqlBot 升级消费 RuleReader `FactBindingRequest 3.0.0` 建立独立 REQ/BIZ/DEV；
  只做契约审计与规划，未实现任何代码，未开始 V3 intake 或 Phase 4R 编排实现。
- 读取的上游权威来源（`D:\Python\pyWorkspace\RuleAgent`，全程只读，未修改该仓库，未读取私有
  `RuleDataReferences` bundle）：其 `AGENTS.md`、`README.md`、`PROG-20260906`、
  `BIZ-20260905-02`（`APPROVED_PATH_B`）、`contracts/fact-binding-request-3.0.0.schema.json`、
  `REQ-20260906-01`/`DEV-20260906-02`（MongoDB Schema v5 持久化规格）。
- 固定证据（2026-09-06 观察）：上游 HEAD `65a96846b3ae991b42a8159dbbee43a9bbe15846`；V3 契约
  文件在该 HEAD 下均为**未跟踪工作区文件**，无法固定到 commit——以文件 SHA-256 为冻结锚点并
  把"上游未提交"登记为阻断项。`contracts/fact-binding-request-3.0.0.schema.json`
  （`$id=urn:rulereader:fact-binding-request:3.0.0`）SHA-256
  `2c5e4603cda243a84f5ec1c47ee65d3e377598fed86b1ddab5a167a2c342c566`；伴生 Schema
  `rule-parse-result-3.0.0` `c34a468d…`、`rule-structure-candidate-3.0.0` `fafded54…`、
  `business-confirmed-fact-catalog-3.0.0` `bb2446f2…` 仅作来源证据。交叉复核：上游工作区的
  `fact-binding-request-2.0.0.schema.json` SHA-256 与 SqlBot 冻结值 `38fec6b2…` 一致，上游
  V2 契约未漂移。
- V2/V3 关键差异（详见 REQ 第 3 节）：`ruleRef` 的 `ruleId`→`ruleSetId` 并新增
  `catalogDigest`/`candidatePayloadSha256`；usages 由表达式侧视角改为五阶段/规则/优先级/
  多 outcome 视角；evidence/provenance 结构重组且时间不进 hash；uncertainties `impact` 固定
  `warning`（blocking 结构性不可能）；queryRequirements 无任何 unresolved 状态；顶层六个固定
  const。字段集互不相交，双向严格解析必然失败，禁止降级。
- 新增文档（均 `proposed`，编号未占用）：
  [REQ-20260906-03](../requirements/REQ-20260906-03-fact-binding-v3-intake.md)、
  [BIZ-20260906-02](../decisions/BIZ-20260906-02-fact-binding-v3-authority-boundary.md)、
  [DEV-20260906-03](../architecture/DEV-20260906-03-fact-binding-v3-intake.md)；同步更新
  docs/README 索引（阅读顺序与权威文档列表）、ROADMAP Phase 4R 关键前置表述、README 文档导航。
  三份新文档明确：V2 legacy 保留与路由隔离、一个坏 handoff 整批 fail closed、provider 前零调用
  与 Mongo 零写入、V2 不得移植 gap 语义、`rule_versions_v3` 读取范围外、真实落库/真实读取验证
  均需显式授权。
- 阻断项登记：上游 V3 契约文件未提交（实施冻结副本前需上游提交并复核 SHA-256）；上游 MongoDB
  Schema v5 真实落库未执行（上游状态"离线实现完成、真实写入待授权"，落库前 batch 集合恒为空）；
  真实 MongoDB 只读冒烟需显式授权任务。
- 验证证据：三份新文档与被更新索引的全部相对链接有效（含跨仓库引用仅以文本形式登记，未建立
  运行时依赖）；`uv run ruff check .` 通过；`uv run ruff format --check .` 通过；
  `uv run pytest` → `391 passed`；`git diff --check` 通过。
- 本任务未提交、未推送；工作区中三份 Phase 4R 规划文档（REQ/BIZ/DEV-20260906-02 系列）仍处于
  未跟踪状态，其纳入提交属独立任务；本任务新增的三个文档与索引更新同样未提交。

## FactBindingRequest 3.0.0 intake 离线实现（同日补充）

- 任务边界：按 [REQ-20260906-03](../requirements/REQ-20260906-03-fact-binding-v3-intake.md) /
  [BIZ-20260906-02](../decisions/BIZ-20260906-02-fact-binding-v3-authority-boundary.md) /
  [DEV-20260906-03](../architecture/DEV-20260906-03-fact-binding-v3-intake.md) 实现 V3 intake
  最小纵向切片；未实现 Phase 4R 编排、候选生成变更、SQL Server 访问或 Phase 5B/5C。
- 门禁处理（如实登记）：任务开始时两道前置门禁均未满足（三份文档仍为 `proposed`；上游 V3 契约
  文件仍未提交）。用户在知悉上述阻断后明确指示"继续"；据此按 PROG-20260906 既有先例将
  REQ/DEV 置为 `in_progress`、BIZ 置为 `approved`。上游 commit 缺失无法由本任务消除（禁止修改
  RuleAgent），以来源清单与加载器共同锁定的 Schema SHA-256
  `2c5e4603cda243a84f5ec1c47ee65d3e377598fed86b1ddab5a167a2c342c566` 作为冻结锚点，该阻断项
  保持开放。
- 实现（全部离线）：`src/release_sql_bot/contracts/fact-binding-request-3.0.0.schema.json` 冻结
  副本（LF 规范化保存，加载时还原上游 CRLF 字节后校验哈希）；
  `docs/specs/fact-binding-request-3.0.0-source.json` 来源清单；
  `domain/fact_bindings_v3.py` 独立严格 consumer（镜像上游全部模型校验器：requestId/ruleSetId
  闭包、evidence 双向闭合、factValue 唯一性、五阶段 usage 唯一性、`impact=warning` 固定等）；
  `domain/fact_binding_handoffs_v3.py` 单文档 batch wrapper 契约；
  `application/handoff_intake_v3.py` 门禁（Schema/身份/payload hash/batch hash/唯一性，整批
  fail closed，合法批次固定 `readyForMetadataResolution`）；`application/ports/handoffs.py` V3
  只读端口；`infrastructure/database/mongodb.py` 精确 ruleVersion 单文档只读查询（零写方法）；
  `api/app.py` 新增 `GET /api/v1/fact-binding-handoffs/v3`（200/404/502/503）；
  `config/settings.py` 与 `.env.example` 新增 `RSB_MONGODB_FACT_BINDING_BATCH_COLLECTION`
  （默认 `fact_binding_handoff_batches_v3`）。
- 测试：新增 `tests/contract/test_fact_binding_v3_contract.py`（14 项：冻结哈希、无损
  round-trip、extra=forbid/snake_case/强转拒绝、blocking/derived/unresolved 拒绝、V2/V1/V3
  双向不可解析、来源清单、合成 fixture 过冻结 Schema、batch JSON 可解析）与
  `tests/unit/test_handoff_intake_v3.py`（16 项：合法批次、确定性排序、七类篡改 fail closed、
  重复身份、异版本批次、时间字段不参与哈希、not found/invalid/unavailable 稳定错误）；扩展
  `tests/integration/test_api.py`（3 项：V3 端点 200 且 provider 零调用、404/502/503 安全映射、
  数据库关闭时 503）；新增合成脱敏 fixtures 与 `tests/v3_handoff_support.py`。
- V2 回归：唯一行为交叉点是 `MongoRuleStore` 生命周期测试新增的 V3 集合句柄断言（共享客户端
  预期结果），已按 DEV 模块清单同步；V2 模型、V2 intake、候选生成与存储行为零改动。
- 验证证据：`uv run pytest` → `424 passed`（原 391 + 新增 33）；`uv run ruff check .` 与
  `uv run ruff format --check .` 通过（178 files already formatted）；`git diff --check` 通过；
  修改文档相对链接检查通过。
- 安全边界：未连接真实 MongoDB/SQL Server、未调用 DeepSeek、未生成或执行 SQL、未读取 `.env`
  或私有 bundle、未读取兄弟仓库运行时文件、未修改 RuleAgent；intake 无 provider 装配、无任何
  Mongo 写路径；全部产物固定 `executable=false`；不选择"最新"或近似版本。
- 保持开放（不得视为已完成）：真实 handoff 未落库（上游 Schema v5 真实写入待授权）、上游 V3
  契约文件未提交（实施前观察到的未跟踪状态）、真实 MongoDB 读取验证未执行、持久化与 intake
  不代表审批或可执行。
- 本任务未提交、未推送，等待人工审查。

## Phase 4R Milestone 0 跨仓库基线审计与 V3 下游对齐规划（同日补充）

- 任务边界：只读核查 SqlBot 与 RuleAgent（严格只读）基线，登记 V3 intake 与 Phase 2G/3/4/
  存储之间的契约断点，并为其建立下游对齐 REQ/BIZ/DEV 规划；未实现任何业务代码，未连接任何
  外部系统，未修改 RuleAgent，未提交或推送。
- SqlBot 基线复核（全部与登记值一致）：main 工作树干净、领先 origin/main 10 提交、HEAD
  `b3e3d35` 包含 `0afee5c`/`146b6b2`；`uv run pytest` → **424 passed**（合成脱敏离线基线，
  不构成真实数据验证；唯一 warning 为 `.pytest_cache` ACL 问题，另行处理不阻断）。
- RuleAgent 只读基线复核（`git -c safe.directory` 只读检查，未修改该仓库）：
  - HEAD `65a96846b3ae991b42a8159dbbee43a9bbe15846`；`contracts/fact-binding-request-3.0.0.schema.json`
    为**未跟踪工作区文件**，大量 V3 代码/文档/测试未提交——不得把未提交 HEAD 写成 Schema
    来源 commit；
  - V3 Schema 原始字节 SHA-256 双工具复核均为
    `2c5e4603cda243a84f5ec1c47ee65d3e377598fed86b1ddab5a167a2c342c566`，与 SqlBot 冻结值
    一致，契约字节未漂移，但**尚无上游 commit 锚点**；
  - 基线漂移（如实登记，不得沿用旧结论）：`pytest` 实测 **174 passed, 6 deselected**（其
    PROG-20260906 记录 173/5；+1/+1 来自未提交工作区在记录后的继续演进，含
    `tests/unit/test_deepseek.py`、`tests/integration/test_mongodb.py` 新增用例）；
    `ruff check` 2 个错误（未跟踪临时脚本 `.scratch_find.py` 的 RUF002）；
    `ruff format --check` 1 个文件需重排版（未跟踪的
    `scripts/persist_report_release_v3_delivery.py`）；`mypy` 与 `pip check` 通过。
    以上属上游在途工作区状态，不构成 SqlBot 行动项。
- 设计缺口登记（[BUG-20260906-01](../bugs/BUG-20260906-01-v3-phase4r-downstream-contract-gap.md)，
  `proposed`）：“完成 V3 intake”只解决读取与契约门禁，不等于 V3 可进入 Phase 2G；
  `FactBindingRequestV3` 不能传给 `ResolveMetadataRequestV2`（其 `binding_request` 固定
  `FactBindingRequestV2`）；V3 usages 的 stage/ruleCode/priority/outcome 语义不能裁剪成 V2
  expressionSide/operator；V3 不能包装/转换/降级为 V2；现有 V2 candidate、静态报告与 Phase 5A
  输入链均绑定 V2 generation request；`CandidateTemplateStore.save` 只接受
  `SqlTemplateCandidateV2`。Phase 4R 既有 REQ/DEV 把“V3 intake 已完成”当作后续链路充分条件，
  属可复现设计缺口；缺口修复前不实现 Phase 4R 编排服务、不生成真实候选。
- 新增文档（编号当日确认未占用；REQ/BIZ/DEV 均 `proposed`，BUG 按模板登记为未完成状态）：
  [REQ-20260906-04](../requirements/REQ-20260906-04-v3-downstream-pipeline-alignment.md)、
  [BIZ-20260906-03](../decisions/BIZ-20260906-03-v3-downstream-authority-boundary.md)、
  [DEV-20260906-04](../architecture/DEV-20260906-04-v3-downstream-pipeline-alignment.md)、
  [BUG-20260906-01](../bugs/BUG-20260906-01-v3-phase4r-downstream-contract-gap.md)。
  规划冻结要点：独立 V3 下游契约链（禁复用 V2 请求/报告/候选契约，允许复用版本无关算法与
  物理元数据 DTO）、`ProjectBindingContextV3` 与独立 V3 快照契约（字段级等价映射）、
  `ResolveMetadataRequestV3`/`BindingResolutionReportV3`（含 usage 摘要哈希、禁调用
  `analyze_binding_gaps_v2`）、`GenerateSqlCandidateRequestV3`/独立 Prompt/
  `SqlTemplateCandidateV3`（结构化 coverage，provenance 闭包含 `catalogDigest`/
  `candidatePayloadSha256`）、`ValidateSqlCandidateRequestV3`/V3 静态报告（禁只按
  conditionId 集合相等宣称覆盖）、独立 V3 store（保留 insert-only/唯一 hash/幂等/pending
  不变量）、Phase 5A 对齐不前移、V2/V3 路由显式隔离、10 类测试矩阵与里程碑 M0–M6
  （各含前置条件、DoD、失败语义、测试范围）。
- 既有文档最小修订（不删改历史事实，以“后续审计修订”章节登记）：
  [REQ-20260906-02](../requirements/REQ-20260906-02-real-upstream-handoff-evidence-loop.md)
  与 [DEV-20260906-02](../architecture/DEV-20260906-02-real-handoff-evidence-loop-orchestration.md)
  各新增第 12/15 节（V3 intake 降级为必要条件、V2 模块复用表不适用于 V3、里程碑顺序修订、
  M0 基线刷新）；[ROADMAP](../ROADMAP.md) Phase 4R 追加审计修订块；docs/README 阅读顺序与
  权威文档列表、根 README 导航与 V3 下游能力澄清同步。
- Phase 4R 修订后里程碑顺序：M0 基线审计（本轮完成登记）→ V3 下游链 M1 授权上下文/快照 →
  M2 元数据解析 → M3 候选生成 → M4 静态门禁 → M5 候选存储 → M6 编排与证据包 → 原 Phase 4R
  Milestone 1–8（对象切换为 V3 契约，DoD 语义不变）。
- 仍需用户明确授权的事项：上游 RuleAgent V3 契约/代码的独立审查与提交（取得真实 commit SHA
  并复核 SHA-256，一致后才能把 source manifest 更新为已提交来源；任何字节变化立即停止并另立
  契约差异评审）；上游 Schema v5 migration 与真实 batch 写入；`ProjectBindingContextV3`/
  V3 快照的批准流程确认；V3 下游实施任务启动；在线 provider 调用与真实存储验证（各自独立
  授权）。
- 验证证据：`uv run ruff check .` → All checks passed；`uv run ruff format --check .` 通过；
  `uv run pytest` → 424 passed；`git diff --check` 通过；本次新增/修改文档的全部 Markdown
  相对链接检查通过；`git status --short --branch` 见最终报告。
- 本任务未连接 MongoDB/SQL Server/DeepSeek/飞书或私有 bundle、未运行 RuleAgent 任何持久化
  脚本、未修改 `src`/`tests`/`contracts`/配置/`.env`、未清理 `.pytest_cache` ACL、未提交、
  未推送，等待人工审核。

## 人工审查意见修订：V3 下游对齐规划（同日第二次补充）

- 任务边界：按人工审查意见修订既有规划文档（仅允许的 10 个文档），不实现代码、不新增需求、
  不连接外部系统、不开始 M1；未提交、未推送。
- RuleAgent 审查时基线刷新（2026-09-06 19:55–19:56，全部命令实际只读执行）：
  `git status --short --branch` → `## main...origin/main`、94 个未提交/未跟踪条目（V3 契约/
  代码/文档仍大量未提交）；`pytest -q` → **174 passed, 6 deselected**；`ruff check .` →
  **All checks passed!**；`ruff format --check .` → **176 files already formatted**；
  `mypy` → **Success: no issues found in 61 source files**；`pip check` → **No broken
  requirements found**。与人工复核观察完全一致；此前登记的 2 个 ruff 错误与 1 个待重排版
  文件属于**较早观察**（`.scratch_find.py` 现已不存在于工作区），不得再描述为当前状态；
  旧观察记录按审计要求原样保留，未删除。
- 审查问题逐项处理（历史记录：本节所述契约名称与排序键已在同日第二轮审查修订中统一为
  `HandoffClosureV3`/`RepositoryVerifiedHandoffV3` 与 `usage_traceability_sha256` 完整
  排序键，见下文第二轮章节；此处按当时状态原样保留）：
  1. **handoff attestation 证明闭包**（DEV-20260906-04 新增 5.1 节、REQ-20260906-04 目标 1/
     冻结要点 3/不变量 5、BIZ-20260906-03 决策 11）：`ResolveMetadataRequestV3` 增加
     `handoff_attestation` 字段（`VerifiedHandoffRequestV3`，`schemaVersion="1.0.0"`），
     携带 ruleVersion/requestId/factCode/payloadSha256/batchSha256/contractSchemaId/
     contractSchemaSha256/intakeStatus/精确 payload，与 `FactBindingHandoffIntakeBatchV3`/
     `FactBindingHandoffIntakeRequestV3` 逐字段逐哈希闭合（8 条闭包规则冻结）；provider 与
     store 前全部重新校验，任一哈希不一致零调用；客户端自报 ready 不构成证明；真实编排从
     仓储精确读取 batch，纯离线测试可用完整合成 attestation；candidate/静态报告/证据包保留
     `batch_sha256`/`payload_sha256`；不得用 V2 `BindingGapReport` 替代。
  2. **usage 六元组**（DEV 5.4/6/7/8 节、REQ 目标 7/冻结要点 5/6、BIZ 决策 3）：权威身份
     统一为完整六元组（stage/ruleCode/priority/conditionId/conditionPath/outcome），REQ/
     BIZ/DEV/测试矩阵四处一致；`usages_traceability_sha256` 规范在本轮冻结（六字段投影、
     `(stage, priority, conditionId)` 排序键、conditionId 唯一为重复身份规则、canonical
     JSON 规则复用）；traceability linkage 与 AST-derived evidence 分界明确——AST 不能证明
     stage/ruleCode/priority/outcome 业务语义；Phase 4 检查更名为 usage traceability /
     reference integrity；候选本体保留完整六元组 + 摘要 + `handoff_refs` 强引用，仅存摘要
     不合格。
  3. **契约版本表**（DEV 新增 4.3 节、REQ 冻结要点 8、BIZ 决策 12）：FBR `contractVersion
     =3.0.0` 只属上游载荷；attestation/context/snapshot/解析请求/解析报告/静态报告/存储
     文档包装首版 `1.0.0`（理由：延续对应 V2 对象先例，独立演进）；`SqlTemplateCandidateV3`
     首版 `3.0.0`（显式登记为候选契约自身版本、延续 V2 候选先例，非机械继承）；
     `ruleRef.schemaVersion` 是唯一表达所引用规则契约版本的字段；V2/V3 隔离按类型与引用
     闭包断言，不按版本号数字；新增测试矩阵第 11 项防机械继承回归。
  4. **双顺序分离**（DEV 第 1 节与 M3–M6、REQ 目标 11/不变量 7/验收 7、BIZ 决策 13）：
     实施顺序可为 M4 先于 M5；运行状态顺序冻结为既有
     `candidateGenerated → candidateStored → staticPassed`（生成成功后先落库并记录存储
     outcome，再对同一 candidate 运行 Phase 4）；静态 blocked 候选保留审计记录、不删除/
     覆盖/修改；保持运行顺序无需新 BIZ 决策，已作为 BIZ 决策 13 登记冻结。
  5. **context 生命周期矛盾修复**（DEV 3.2 节、REQ 冻结要点 1、BIZ 决策 5）：删除“置旧
     记录 superseded”与“禁止 update”并存的表述；冻结方案——业务载荷 insert-only/不可变，
     新版本 `contextVersion+1` 新文档，旧载荷永不原地改写；active/superseded 用独立生命
     周期事件或 active pointer + compare-and-swap 表达并有独立审计；M1 只做契约与纯计算
     校验，不实现 MongoDB context 仓储（存储设计列为 DEV 开放问题 2，M6 前冻结）。
  6. **M0 状态口径修正**（DEV M0、REQ-20260906-02 §12.4、DEV-20260906-02 §15.3、REQ-04
     验收 8、ROADMAP Phase 4R）：区分“审计子任务已完成”与“M0 整体已收口”；M0 整体为
     `in_progress`（blocked on upstream anchor）——规划文档批准并提交、RuleAgent V3 契约
     commit、上游 SHA-256 复核三项完成前不收口，M1 不得开始。
- 验证证据：`uv run ruff check .` → All checks passed；`uv run ruff format --check .` 通过；
  `uv run pytest` → 424 passed（离线合成基线）；`git diff --check` 通过；Markdown 相对
  链接检查全部有效；`git status` 确认本轮变更仅涉及允许的 10 个文档（README 与 docs/README
  本轮无需改动）。
- 本任务未实现任何 V3 下游代码、未修改 V3 intake 代码、未连接 MongoDB/SQL Server/DeepSeek/
  飞书或私有 bundle、未读取或输出 `.env`、未运行 RuleAgent 持久化脚本/migration/服务/
  integration 测试、未修改或暂存 RuleAgent、未提交、未推送，等待下一次人工审核。

## 第二轮人工审查修订：handoff 真实性分层、usage 唯一性更正与快照口径（同日第三次补充）

- 任务边界：按第二轮人工审查意见修订既有规划文档（仅允许的 10 个文档中实际涉及的 6 个），
  不实现代码、不开始 M1、不修改 RuleAgent；未提交、未推送。
- 代码事实核查（只读，未修改任何代码）：`domain/fact_bindings_v3.py` 的 usages 重复门禁是
  **四元组 `(stage, ruleCode, conditionId, conditionPath)`**，不保证 `conditionId` 单独
  唯一；上游冻结 Schema（2026-09-06 核对）对 `usages` 数组只声明 `minItems: 1`，无
  `uniqueItems`、无顺序语义，首个命中语义由 `priority` 字段承载。第一轮文档中
  “conditionId 全请求唯一 / consumer 强制唯一 / 用 conditionId 消除排序歧义 / 防御性拒绝
  重复 conditionId”的陈述全部更正删除。
- RuleAgent 审查时基线刷新（全部命令实际只读执行，`git -c safe.directory` 只读检查）：
  - **人工复核观察**（本轮审查意见转述）：189 passed, 6 deselected；ruff check 通过；
    format 179 files；mypy、pip check 通过；git 条目 99。
  - **本任务快照**（2026-09-06 21:02:21–21:02:30，开始/结束时间实际记录）：git status
    `## main...origin/main`、99 条目；`pytest -q` → **201 passed, 6 deselected**；
    `ruff check .` → All checks passed；`format --check .` → 179 files already formatted；
    `mypy` → Success（61 source files）；`pip check` → No broken requirements found。
  - 与人工复核观察的差异：pytest +12（189→201），其余一致。只登记开始/结束观察差异，
    不猜测修改来源，不追改上游；**M0 继续 `in_progress` / blocked on upstream anchor**。
- 文档口径调整：RuleAgent 工作区活跃变化期间，各组数字一律为“带时间戳的观察快照”而非
  长期当前事实，不再把任何一次快照的测试数量写成固定跨仓库门禁；M0 的稳定门禁只有
  “工作树经独立审查、V3 契约与实现已提交、取得真实 commit SHA、Schema SHA-256 在该提交
  树上重新核对、必要检查在该提交树上通过”（DEV-20260906-04 第 13 节快照 1–5 序列保留）。
- 审查问题逐项处理：
  1. **内容闭包与仓储真实性分层**（DEV 5.1 节重写、REQ 目标 1/不变量 5/冻结要点 3、
     BIZ 决策 11、BUG 修复方向 1）：`HandoffClosureV3`（纯计算可序列化内容闭包）只能证明
     内部一致，不能证明 batch 真实存在、由 RuleReader 写入、intakeStatus 来自真实 intake
     或属于当前真实运行；`RepositoryVerifiedHandoffV3` 是受信应用服务在同一次调用中经
     `FactBindingHandoffBatchRepositoryV3` 重读 batch、重执行 intake、选择唯一 request 并
     逐字段逐哈希比较后的内部结果，不可从 JSON 反序列化；`ResolveMetadataRequestV3` 保持
     纯计算并携带 closure，`metadataResolved` 不宣称仓储真实性；一切外部副作用路径
     （provider/store/真实证据包/未来真实生成入口）必须先在同一次调用中完成仓储背书，仅
     重算调用方 hash 不算；candidate/静态报告/证据包中的 batch/payload hash 是追溯引用
     而非真实性证明，真实运行证据须记录 repository verification 阶段与结果。
  2. **M3 安全方案冻结**（DEV 6.2 节、REQ 目标 6、BIZ 决策 11）：采用推荐方案 a——生成
     服务直接依赖只读 handoff repository，在 provider 前于服务自身完成仓储重读与背书
     （安全边界可在 provider 调用服务自身被验证）；方案 b（仅 M6 编排调用、无独立
     wire 入口）保留为备选并要求登记放弃原因。
  3. **usage 唯一性与合并禁止**（DEV 5.4 节、REQ 目标 7、BIZ 决策 3、BUG 修复方向 2）：
     四元组事实更正；同 conditionId 不同 stage/outcome 或不同 ruleCode/conditionPath 的
     多个 usage 合法且不得合并；不得为下游方便收紧上游契约（除非另立跨仓库契约变更
     需求）。
  4. **digest 名称与算法冻结**（DEV 5.4 节、REQ 目标 7、BIZ 决策 3）：全仓库唯一名称
     `usage_traceability_sha256`（Python/domain）与 `usageTraceabilitySha256`（wire），
     原混用的 `usages_sha256`/`usages_traceability_sha256` 全部清除（全仓 grep 复核为
     零残留）；算法冻结为六字段投影 + 完整稳定排序键（stage 业务顺序
     stateGuards→prerequisites→eligibility→postGates→exclusions，其后 priority/ruleCode/
     conditionId/conditionPath/outcome 升序，明确不得只用 stage+priority+conditionId）+
     既有 canonical_sha256；evidenceIds 与时间字段不进摘要；摘要不证明 AST 或业务语义、
     不能替代逐项六元组比较；若上游未来明确 usages 原顺序具有业务语义，必须停止排序并
     登记阻断性开放问题，不得自行选择。
  5. **测试计划扩充**（DEV 矩阵 3–7、REQ 矩阵 3–7）：完全自洽但从未存在于仓储的伪造
     closure 在真实 provider 路径被拒绝；仓储 batch hash 与调用方 closure 不一致 →
     provider/store 均 0 调用；仓储无 batch → 均 0 调用；合成 closure 夹具结论明确非真实
     仓储证明；同 conditionId 多 usage（跨 stage/outcome、跨 ruleCode/path）合法不合并；
     六元组任一字段变化改变摘要；数组重排不变摘要；完全相同四元组按现行 consumer 规则
     拒绝。
- 状态边界：M0 保持 `in_progress` / blocked on upstream anchor，本轮修复不完成 M0；M1
  不得开始；REQ/BIZ/DEV 保持 `proposed`，BUG 保持未完成状态；四份新文档仍为工作区未跟踪
  文件，不得描述为“已纳入仓库提交”。
- 验证证据：`uv run ruff check .` → All checks passed；`uv run ruff format --check .` 通过；
  `uv run pytest` → 424 passed（离线合成基线）；`git diff --check` 通过；Markdown 相对
  链接检查全部有效；`git status` 确认变更仅涉及允许清单内文档（README、docs/README、
  ROADMAP 本轮无需改动）。
- 本任务未实现任何代码或测试、未修改既有 V3 consumer 的唯一性规则、未修改 RuleAgent、
  未连接 MongoDB/SQL Server/DeepSeek/飞书或私有 bundle、未读取或输出 `.env`、未运行
  RuleAgent migration/持久化脚本/服务/integration、未提交、未推送，等待下一轮人工审核。
