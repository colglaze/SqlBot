# AGENTS.md

本文件只规定 AI 编码 Agent 在本仓库中的长期工作约定。产品范围、业务规则、架构设计和实施进度以 `docs/` 为准，不在这里重复维护。

## 1. 基本行为

- 面向用户的说明、文档和注释默认使用中文；代码标识符、API 字段、数据库字段和提交信息使用英文。
- 开始任务前先阅读 `README.md`、`docs/README.md`、当天或最近的 `docs/progress/PROG-*.md`，再按任务读取相关 REQ、DEV、BIZ 和规格文档。
- 先检查现有代码、测试和文档，不猜测仓库中可以查到的事实。
- 只修改当前任务需要的内容；不顺手重构，不提前实现未进入当前 Phase 的功能。
- 遇到业务语义、SQL 方言、源表结构或权限边界不明确时，记录假设并阻止危险路径，不得让模型自行补全生产事实。

## 2. 文档与冲突处理

- 用户当前明确要求优先于仓库文档；稳定的工程约定由本文件维护。
- 文档职责：REQ 定义“做什么和如何验收”，BIZ 记录业务决策，DEV 定义“如何设计”，规格文档定义机器或数据契约，PROG 记录当前事实。
- 代码、测试与高优先级文档不一致时，必须指出差异，并在同一任务中同步可确定的文档；不能确定时记录为阻塞项。
- 产品范围、架构、MongoDB 数据模型、规则 JSON 或 SQL 审核流程发生变化时，先更新或新增对应文档，再实现代码。
- 新文档使用 `REQ-YYYYMMDD-NN-*`、`DEV-YYYYMMDD-NN-*`、`BIZ-YYYYMMDD-NN-*`、`BUG-YYYYMMDD-NN-*`、`PROG-YYYYMMDD.md` 命名，并补充到 `docs/README.md` 索引。

## 3. 核心业务不变量

- MongoDB 中已批准规则和 SQL 模板的业务载荷是不可变审计记录；生命周期状态只可按受审计状态机变化，内容修订必须创建新版本，禁止原地覆盖。
- 新 SQL 必须可追溯到当前规则、基线规则、基线 SQL、项目上下文、模型配置和生成运行。
- LLM 输出永远是不可信候选物。未通过静态校验、受限环境验证和人工审核的 SQL 不得发布或用于生产查询。
- 普通生成结果只能包含只读查询；分阶段方案仅可使用会话级临时表。禁止永久 DDL、DML、存储过程、外部访问和跨允许范围查询。
- SQL 必须使用参数绑定，不能把规则值直接拼接进 SQL。
- 执行校验和正式查询使用最小权限只读账号，并设置超时、行数/结果大小限制和审计信息。
- 每条规则条件都必须有稳定 `condition_id`，生成结果必须提供“规则条件 → SQL 片段/阶段”的覆盖映射。

## 4. 计划中的代码边界

- `domain/` 不依赖 Web 框架、MongoDB/SQL 驱动或具体 LLM SDK。
- API 层不直接访问 MongoDB、不直接执行 SQL、不直接调用模型。
- 仓储、LLM、SQL 解析器和目标数据库通过端口/适配器隔离。
- 配置集中加载和校验；业务代码不得零散读取环境变量。
- SQL 方言差异放在 dialect adapter 中，禁止把方言分支散落在领域逻辑里。
- 规则解释、SQL 生成、SQL 校验、执行计划和发布审批是独立步骤，不合并成不可观测的单次模型调用。

## 5. 实施工作流

1. 从最近的 PROG 文档确认 Phase、前置依赖和阻塞项。
2. 在 REQ/BIZ 中定位业务语义和验收标准，在 DEV/规格中定位接口与约束。
3. 以最小可验证纵向切片实现：契约 → 领域逻辑 → 适配器/API → 测试 → 文档和进度。
4. 涉及生成 SQL 时，同时补齐正常、边界、空值、规则变化和超时/降级测试。
5. 完成后更新 PROG 的状态、验证证据、遗留风险和下一步。

## 6. 测试与完成标准

- 使用 Python 3.11 和 `uv` 管理项目；首次安装运行 `uv sync`。
- 权威检查命令：`uv run ruff check .`、`uv run ruff format --check .`、`uv run pytest`。
- 服务启动命令：`uv run release-sql-bot serve`；安全配置检查：`uv run release-sql-bot check-config`。
- 规则 JSON 必须通过 JSON Schema 校验；示例也必须纳入自动测试。
- SQL 测试至少覆盖：语法/单语句限制、表和列白名单、只读限制、参数化、条件覆盖、方言适配和临时表清理。
- 生成链路测试使用固定夹具或 mock，不依赖真实生产库和在线模型才能完成基础回归。
- “代码可运行”不是完成。相关测试、文档、迁移/索引说明和 PROG 证据缺一不可。

## 7. 安全与 Git

- 不记录或提交 `.env*`、密钥、生产连接串、真实业务数据、数据库导出、日志、模型原始敏感提示或构建产物。
- 测试数据必须合成或脱敏。
- 未经用户明确授权，不推送远程、不发布版本、不连接或修改生产系统。
- 不将参考项目的源代码直接复制进本项目；若引入依赖或派生代码，先核对许可证并记录来源。

## 8. Code Review Rules

- 标记任何可能绕过人工审批、在批准后改变 SQL 文本、原地覆盖版本或放宽数据访问范围的改动。
- 标记任何未对 LLM 输出做 AST/语义校验便直接执行的路径。
- 标记任何永久 DDL/DML、未绑定参数、未限制表范围、无超时或临时表未在同一会话清理的 SQL 路径。
- 标记规则条件未进入覆盖矩阵，或 `NOT`、空值语义、时间边界在规则与 SQL 间不一致的情况。
