[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "release-sql-bot"
version = "0.1.0"
description = "Auditable release-rule SQL generation and review service"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.139.2,<0.140",
    "langgraph>=1.2.9,<1.3",
    "pydantic-settings>=2.14.2,<2.15",
    "uvicorn>=0.51,<0.52",
]

[project.scripts]
release-sql-bot = "release_sql_bot.__main__:main"

[dependency-groups]
dev = [
    "httpx2>=2.9.1,<3",
    "jsonschema[format]>=4.26,<5",
    "pytest>=9.1.1,<10",
    "ruff>=0.16,<0.17",
]

[tool.hatch.build.targets.wheel]
packages = ["src/release_sql_bot"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
